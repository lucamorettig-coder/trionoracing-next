import * as React from "react";
import { FondamentaColori } from "trionoracing-next";

/** Scheda di riferimento: rende i token reali del DS, non una loro copia. */
export const Riferimento = () => <FondamentaColori />;
