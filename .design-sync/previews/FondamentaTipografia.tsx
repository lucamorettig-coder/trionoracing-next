import * as React from "react";
import { FondamentaTipografia } from "trionoracing-next";

/** Scheda di riferimento: rende i token reali del DS, non una loro copia. */
export const Riferimento = () => <FondamentaTipografia />;
