# Handoff sessione — 2026-07-13

> Per riprendere in una nuova sessione Claude Code su questo repo. Legge come un briefing: cosa è successo, dove siamo, cosa fare dopo.

## TL;DR

Il restyle APEX (dark, "Velodromo Notturno") è **live su 2 pagine**: Home ed **/la-scuola**, entrambe in produzione e rifinite dopo feedback diretto. Nel mezzo si è dovuta risolvere un'emergenza infrastrutturale (Cloudinary bloccato). **Zero PR aperte**, `main` pulito. Prossimo passo naturale: continuare il rollout dell'ombrello EVO-037 su `/marathon-209` (serve un input tuo: il file GPX del percorso) oppure su `/chi-siamo` + `/gli-amatori-triono`.

---

## Cosa è successo in questa sessione (in ordine)

1. **EVO-038** (fondazione DS v2 APEX + restyle Home) — chiusa, mergiata, verificata live.
2. **EVO-039** (restyle `/la-scuola` in livrea Scuola) — pianificata con `evolutive-workflow` completo (fasi 0-8), implementata a wave con subagenti Sonnet 5 in worktree isolati. **Bug di processo scoperto**: alcuni subagenti mettevano i quality gate in background e restavano ad aspettare notifiche invece di completare in foreground → terminavano **senza committare**. Fix: passati a modalità "edit-only" (il subagente modifica solo il file, il planner fa gate+commit centralizzati). Pattern documentato in `AGENTS.md`.
3. **Incidente Cloudinary** (durante lo smoke di EVO-039): il cloud `duezeronove` (condiviso con duezeronove.it, piano Free) aveva esaurito la quota crediti → delivery bloccata **account-wide** (404 anche su asset appena caricati, ma l'Admin API restava viva). Causa aggravante: un doppio rename del cloud name fatto in una sessione precedente aveva invalidato tutta la cache CDN, accelerando lo sforamento quota.
4. **EVO-040** (migrazione Cloudinary) — nuovo account dedicato `u5hvesvu`. Scoperta tecnica chiave: `generate_archive` in `mode=download` restituisce lo ZIP **dall'endpoint API**, che bypassa la delivery bloccata — così si sono potuti esportare i 32 asset (+ 1 OG per la 209) dal vecchio cloud nonostante il blocco, e ricaricarli sul nuovo preservando i public_id. Codice + Airtable (Sfondi Video DEV/PROD + base 209) ripuntati. **Scoperta collaterale**: `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME` non era usato da nessuna parte nel codice → zero modifiche env Vercel necessarie per il go-live.
5. Merge coordinato: EVO-040 prima (ripara i media ovunque nel sito) → rebase di EVO-039 sopra → verifica che kit/galleria si popolassero → merge EVO-039.
6. **EVO-041** (rifiniture `/la-scuola`) — **due round** di fix su feedback diretto dell'utente, con verifica screenshot ad ogni passo (`scripts/dev-shot.mjs`, non il preview MCP che è inaffidabile su questo progetto):
   - **Round 1**: hero da card-chiara-con-canvas-al-cursore a stage scuro con mascotte statiche; kit/allenarsi con fix bordo (bianco + `mix-blend-multiply`); CtaBand iscrizione — **qui c'è stato un fraintendimento**: l'utente voleva il pattern geometrico RIMOSSO, è stato inteso come "va intensificato".
   - **Round 2** (dopo review screenshot dell'utente): hero con **solo Vittoria** (l'asset ricavato da poster video aveva scontorno sporco → sostituito con asset Adobe pulito dalla libreria Cowork); scoperto e fixato un **bug percettivo reale**: l'animazione `.reveal` fa fade su `opacity`, quindi una card bianca a opacity parziale durante lo scroll-in **appare grigia** — sembrava un problema di "colori diversi" ma era l'animazione. Creata `.reveal-slide` (solo `transform`, niente `opacity`) per le superfici chiare su stage scuro. Fix veri: `overflow-hidden` che clippava un pill sporgente, `object-cover` al posto di `object-contain`+padding per eliminare davvero i bordi, e la CtaBand riportata pulita (pattern rimosso come richiesto la seconda volta).
7. **`npx impeccable install`** eseguito su tua richiesta: installata la skill `impeccable` (v3.9.1) a livello progetto in `.claude/skills/impeccable/` — un design-system per craft/audit/polish di UI frontend con sotto-comandi (`craft`, `audit`, `polish`, `animate`, `harden`, ecc.). **Non ancora usata**, solo installata. La cartella è untracked (non ancora deciso se committarla nel repo).

---

## Stato del repo ORA

- **Branch**: `main`, pulito, allineato a `origin/main` (`2a9aaa6`)
- **PR aperte**: nessuna
- **Ultimi commit rilevanti**: EVO-038 → EVO-040 → EVO-039 → EVO-041 → docs, tutti mergiati e live
- **Produzione**: https://trionoracing.it/ — Home e /la-scuola in APEX dark, verificate live
- **Worktree residui** (`.claude/worktrees/*`): alcuni datati (branch EVO-032, EVO-029, EVO-033 e detached HEAD) — **non toccati in questa sessione**, probabilmente scarto di sessioni precedenti; da verificare/pulire se danno fastidio, non urgente

## Ombrello EVO-037 — stato figlie

| Figlia | Stato |
|---|---|
| EVO-038 (fondazione + Home) | ✅ chiusa, live |
| EVO-039 (/la-scuola) | ✅ chiusa, live |
| EVO-041 (rifiniture /la-scuola) | ✅ chiusa, live |
| /marathon-209 | da pianificare — **serve il GPX reale del percorso da te** per l'altimetria (M2), altrimenti si parte con dati fittizi come da DS-APEX |
| /chi-siamo + /gli-amatori-triono | da pianificare |
| /diventa-maestro + /contatti + legali | da pianificare |

L'ombrello resta aperto finché non sono chiuse tutte.

## Cose da sapere prima di continuare

- **Cloud Cloudinary attuale**: `u5hvesvu` (Free, dedicato solo a trionoracing). **Monitora la quota** — è di nuovo un piano Free, l'incidente di questa sessione può ripetersi se il traffico cresce. Non rinominare mai il cloud name (invalida tutta la cache CDN).
- **Pattern anti-stallo subagenti**: se rilanci un flusso a wave con subagenti Sonnet 5 in worktree, preferisci la modalità "edit-only" (il subagente tocca solo il file, niente gate/commit) e fai gate+commit centralizzati tu — evita lo stallo su build/lint in background documentato in `AGENTS.md` (sezione EVO-039+040).
- **Verifica visiva**: usa sempre `node scripts/dev-shot.mjs <path> [opzioni]` (Chrome headless via CDP). Il preview MCP (`Claude_Preview`) si blocca spesso su `chrome-error://` su questo progetto — non affidabile.
- **`.reveal` vs `.reveal-slide`** (nuova, da EVO-041): `.reveal` fa fade+slide, adatta a testo/superfici scure. `.reveal-slide` fa solo slide (no fade), da usare su qualunque card/superficie **chiara** che appare dentro il palco scuro APEX — altrimenti il fade la fa sembrare grigia durante lo scroll.
- **`.apex-card--warm`**: ora è bianco puro (`#ffffff`), non più avorio. Se prossime pagine (marathon-209, chi-siamo) hanno bisogno di superfici chiare, riusa questa classe + `.reveal-slide`, è già collaudata.
- **impeccable**: installata ma non testata. Se la usi sulla prossima pagina, segui il suo setup (`node .claude/skills/impeccable/scripts/context.mjs` per primo, poi la reference del sotto-comando). Potrebbe utilmente affiancare (non sostituire) `evolutive-workflow` per la fase di critique/polish visivo.

## Suggerimento per il prossimo passo

Chiedi all'utente:
1. Se vuole procedere con **`/marathon-209`** (ha bisogno del GPX prima o si parte comunque con dati fittizi?), oppure
2. **`/chi-siamo` + `/gli-amatori-triono`** (nessuna dipendenza esterna, si può partire subito), oppure
3. Provare **`impeccable`** su una pagina già fatta (es. audit/polish su /la-scuola) prima di andare avanti col rollout.
