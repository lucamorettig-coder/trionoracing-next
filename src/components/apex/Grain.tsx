/**
 * APEX DS v2 — Grain del telaio (L−2, sopra il fondale).
 * SVG feTurbulence inline (data URI in apex.css), fixed, animato a steps().
 * Sempre presente sul palco; decorativo → aria-hidden.
 * Server Component, zero JS.
 */
export function Grain() {
  return <div className="apex-grain" aria-hidden="true" />;
}
