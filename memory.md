# Memory — Triono Racing Next.js

> Indice cronologico di tutte le evolutive del progetto. Ogni evolutiva ha un file di dettaglio in `evolutive/EVO-XXX-{slug}.md`.

## Evolutive

| ID | Slug | Titolo | Data inizio | Data fine | Stato | URL produzione | File |
|----|------|--------|-------------|-----------|-------|----------------|------|
| EVO-001 | portale-f3 | Portale F3 — Portale genitori/maestro/admin (ombrello) | 2026-05-21 | — | ombrello | — | [link](evolutive/EVO-001-portale-f3.md) |
| EVO-002 | portale-infra | F3.1 — Setup infra portale (Clerk ruolo-aware + NavBar + webhook) | 2026-05-21 | 2026-05-21 | completata | https://trionoracing-next.vercel.app/portale | [link](evolutive/EVO-002-portale-infra.md) |
| EVO-003 | portale-genitore-core | F3.2 — Area genitore core (dashboard + figli + cert + foto) | 2026-05-22 | 2026-05-22 | completata | https://trionoracing-next.vercel.app/portale | [link](evolutive/EVO-003-portale-genitore-core.md) |
| EVO-004 | portale-iscrizioni | F3.3 — Iscrizioni e pagamenti (wizard + modulistica + SumUp) | 2026-05-22 | 2026-05-22 | completata | https://trionoracing-next.vercel.app/portale/iscrizioni | [link](evolutive/EVO-004-portale-iscrizioni.md) |
| EVO-005 | portale-gare-genitore | F3.4 — Calendario gare genitore | 2026-05-24 | 2026-05-24 | completata | https://trionoracing-next.vercel.app/portale/gare | [link](evolutive/EVO-005-portale-gare-genitore.md) |
| EVO-006 | portale-maestro | F3.5 — Area maestro (lezioni + gare assegnate) | 2026-05-24 | 2026-05-24 | completata | https://trionoracing-next.vercel.app/portale/lezioni | [link](evolutive/EVO-006-portale-maestro.md) |
| EVO-007 | portale-admin | F3.6 — Area admin (dashboard + 11 sotto-pagine) — ombrello | 2026-05-25 | — | ombrello | — | [link](evolutive/EVO-007-portale-admin.md) |
| EVO-008 | migrazione-clerk | F3.7 — Migrazione utenti Supabase → Clerk (+ Disabilita account admin da EVO-020) | 2026-06-06 | — | pronta per implementazione | — | [link](evolutive/EVO-008-migrazione-clerk.md) |
| EVO-009 | kit-scuola | Kit Scuola — vetrina pubblica /la-scuola + immagini nel size picker portale (ombrello — chiuso: EVO-010 + EVO-011 completate) | 2026-05-23 | 2026-06-06 | completata | https://trionoracing-next.vercel.app/la-scuola | [link](evolutive/EVO-009-kit-scuola.md) |
| EVO-010 | kit-scuola-vetrina-pubblica | Kit Scuola — sezione editoriale su /la-scuola + asset condiviso | 2026-05-23 | 2026-05-23 | completata | https://trionoracing-next.vercel.app/la-scuola | [link](evolutive/EVO-010-kit-scuola-vetrina-pubblica.md) |
| EVO-011 | kit-scuola-tab-taglie | Kit Scuola — micro foto kit + taglie reali dallo schema nel TabTaglie portale (fix bug latente 422) | 2026-05-23 | 2026-06-06 | completata | https://trionoracing-next.vercel.app/portale/iscrizioni | [link](evolutive/EVO-011-kit-scuola-tab-taglie.md) |
| EVO-012 | ds-photo-bg-colorate | DS — utility `.photo-bg-{color}` per card colorate + uniformazione 8 card navy | 2026-05-23 | 2026-05-24 | completata | https://trionoracing-next.vercel.app/la-scuola | [link](evolutive/EVO-012-ds-photo-bg-colorate.md) |
| EVO-013 | portale-pagina-pagamenti | Pagina trasversale `/portale/pagamenti` (nata dal QA EVO-004 hotfix #17) | 2026-05-24 | 2026-05-24 | completata | https://trionoracing-next.vercel.app/portale/pagamenti | [link](evolutive/EVO-013-portale-pagina-pagamenti.md) |
| EVO-014 | portale-ux-stato-iscrizioni | Portale UX: stato iscrizione figli + Azioni Rapide condizionali + 5 bug fix | 2026-05-24 | 2026-05-24 | completata | https://trionoracing-next.vercel.app/portale | [link](evolutive/EVO-014-portale-ux-stato-iscrizioni.md) |
| EVO-015 | titoli-descrizione | Titoli pagamento: campo DESCRIZIONE come label primaria + fix "undefinedª rata" | 2026-05-24 | 2026-05-24 | completata | https://trionoracing-next.vercel.app/portale | [link](evolutive/EVO-015-titoli-descrizione.md) |
| EVO-016 | admin-infra-ds | Admin Infra & DS scaffold (Dialog/AlertDialog/DataTable + dashboard A-1 minimal + schema STATO_ISCRIZIONE annullata) | 2026-05-25 | 2026-05-25 | completata | https://trionoracing-next.vercel.app/portale/admin | [link](evolutive/EVO-016-admin-infra-ds.md) |
| EVO-017 | admin-iscrizioni-bambini | Admin iscrizioni A-2/A-3 + bambini A-4 + 4 modal (annulla/forza completa/titolo manuale/segna pagato) | 2026-05-25 | 2026-05-25 | completata | https://trionoracing-next.vercel.app/portale/admin/iscrizioni | [link](evolutive/EVO-017-admin-iscrizioni-bambini.md) |
| EVO-018 | admin-pagamenti-tariffe | Admin pagamenti A-5 + KPI + tariffe A-11 CRUD Q1/Q2/Q3 | 2026-05-25 | 2026-05-26 | completata | https://trionoracing-next.vercel.app/portale/admin/pagamenti | [link](evolutive/EVO-018-admin-pagamenti-tariffe.md) |
| EVO-019 | admin-gare | Admin gare A-6 CRUD manuale (fallback) + A-7 approvazioni iscrizioni + assegnazione maestri | 2026-05-26 | 2026-05-26 | completata | https://trionoracing-next.vercel.app/portale/admin/gare | [link](evolutive/EVO-019-admin-gare.md) |
| EVO-020 | admin-lezioni-maestri-genitori | Admin lezioni A-8 + presenze maestri A-9 con **gestione rimborsi maestri** (nuova tabella PRESENZE_MAESTRI) + genitori A-10 + cambio ruolo **Clerk-sync transazionale con rollback** | 2026-05-26 | 2026-05-26 | completata | https://trionoracing-next.vercel.app/portale/admin/lezioni | [link](evolutive/EVO-020-admin-lezioni-maestri-genitori.md) |
| EVO-022 | scuola-come-iscriversi | Sezione informativa "Cosa occorre per iscriversi" su /la-scuola (funnel 4 step + mockup illustrati + CTA → /portale/iscrizioni) | 2026-06-06 | 2026-06-06 | completata | https://trionoracing-next.vercel.app/la-scuola | [link](evolutive/EVO-022-scuola-come-iscriversi.md) |
| EVO-023 | auth-portale | Restyle pagine auth portale (login + registrati split-screen brand panel su `<SignIn>`/`<SignUp>` Clerk + appearance raffinata + localizzazione IT) + **nuova** pagina recupero-password custom a step | 2026-06-06 | 2026-06-07 | completata | https://trionoracing-next.vercel.app/portale/login | [link](evolutive/EVO-023-auth-portale.md) |
| EVO-024 | privacy-condizioni-analytics | Documenti legali GDPR: Privacy da bozza→definitiva + **nuova** Condizioni di Servizio (sito+portale) + **nuovo** banner cookie consent custom (Consent Mode v2) + Google Analytics 4 (`G-RMGEYC52J0`) post-consenso | 2026-06-07 | 2026-06-07 | completata | https://trionoracing-next.vercel.app | [link](evolutive/EVO-024-privacy-condizioni-analytics.md) |
| EVO-025 | portale-qa-ux-polish | Portale QA/UX polish post-produzione (mobile genitori + admin) — 8 fix da "Check trionoracing.it" | 2026-06-07 | — | pronta per implementazione | — | [link](evolutive/EVO-025-portale-qa-ux-polish.md) |
| EVO-026 | tariffe-corso-solo-mtb | **Nuovo tipo corso "Solo Mountain Bike"** (1 lezione/sett, giovedì — Q1 270€/Q2 180€/Q3 90€) accanto a MTB-BDC, via `TARIFFE.TIPO_CORSO` + wizard a 7 step con scelta corso + **scadenze rate dinamiche** dal mese di iscrizione + admin/vetrina/filtri corso | 2026-06-11 | 2026-06-11 | completata | https://trionoracing-next.vercel.app/portale/iscrizioni | [link](evolutive/EVO-026-tariffe-corso-solo-mtb.md) |
| EVO-027 | portale-ux-redirect-tariffe | Redirect post-creazione figlio → home portale + redirect post-login (navbar auth-aware) + restyle header card tariffe (admin) | 2026-06-12 | 2026-06-12 | completata | https://trionoracing-next.vercel.app/portale | [link](evolutive/EVO-027-portale-ux-redirect-tariffe.md) |
| EVO-028 | codici-sconto | Codici sconto a importo fisso applicabili in iscrizione (prima rata) + pagamento titoli/rate, con periodo di validità, gestiti da admin (CRUD) | 2026-06-12 | 2026-06-13 | completata | https://trionoracing-next.vercel.app/portale/iscrizioni | [link](evolutive/EVO-028-codici-sconto.md) |
| EVO-029 | scuola-restyle-v3 | Restyle + ampliamento corpo `/la-scuola` (modello "v3"): restyle Corsi/Kit/Iscrizione/CTA + **2 sezioni nuove** (Allenarsi a casa, Sicurezza in sella) + mantieni Filosofia/Maestri/Galleria (Galleria mobile a scroll orizzontale) + nuova modalità sfondo DS `BrandBackdrop`. Hero+navbar invariati. Nuove immagini mascotte Higgsfield (casco in sella + mag wheels) | 2026-06-15 | 2026-06-15 | completata | https://trionoracing-next.vercel.app/la-scuola | [link](evolutive/EVO-029-scuola-restyle-v3.md) |
| EVO-030 | profilo-genitore-obbligatorio | **Profilo genitore obbligatorio per iscrizione**: step condizionale "I tuoi dati" come Step 01 del wizard (solo se profilo incompleto → 7/8 step, sequenza dinamica per chiave) con validazione CF; **guard server-side anti-bypass** (422 `PROFILO_INCOMPLETO`); banner soft ember in dashboard; campi profilo `required`+CF. Nessuna modifica schema. _(Mergeata come "EVO-029" in PR #83 per collisione numerazione, rinumerata EVO-030)_ | 2026-06-23 | 2026-06-23 | completata | https://trionoracing-next.vercel.app/portale/iscrizioni/nuova | [link](evolutive/EVO-030-profilo-genitore-obbligatorio.md) |
| EVO-031 | deindex-vercel-app | **De-indicizzazione `*.vercel.app` + canonical sul dominio (chiude D-27)**: `SITE_URL` → `https://trionoracing.it` (apex) → propaga canonical/`og:url`/robots/sitemap al dominio reale; **308 in `proxy.ts`** per gli host di **produzione** `*.vercel.app` → apex (path+query preservati, env-gated `VERCEL_ENV==="production"` → **preview intatte**). DNS cutover già fatto. `www`→apex rimandato (rischio cookie Clerk). Nessuna modifica schema. | 2026-06-24 | 2026-06-24 | completata | https://trionoracing.it | [link](evolutive/EVO-031-deindex-vercel-app.md) |
| EVO-032 | sumup-descrizione-transazioni | Descrizione transazioni SumUp con riferimento bambino/causale (era hardcoded "Pagamento iscrizione Triono Racing") — nuovo helper `descrizioneCheckoutSumUp` in `portale-utils.ts`, solo campo `description` del checkout, zero fetch aggiuntivi | 2026-07-08 | 2026-07-08 | completata | https://trionoracing.it | [link](evolutive/EVO-032-sumup-descrizione-transazioni.md) |
| EVO-033 | report-presenze-maestri | Report rimborsi/presenze maestri generabile da admin (`/portale/admin/presenze-maestri`), export PNG branded navy Triono in 2 varianti (Amministrazione con Importo / Maestri senza), dati da `PRESENZE_MAESTRI` (per maestro: Lez. MTB / Lez. Strada / Gare / Totale / Importo + riga Totale + footer club/stagione). _(Pianificata come "EVO-032" — rinumerata EVO-033 per collisione con PR #87/#88 mergiate durante la pianificazione)_ | 2026-07-09 | — | in implementazione | — | [link](evolutive/EVO-033-report-presenze-maestri/EVO-033-report-presenze-maestri.md) |
| EVO-034 | iscrizione-sospesa | **Stato `SOSPESA` per morosità**: quando un'iscrizione altrimenti COMPLETA ha un titolo scaduto da >30gg, la formula `STATO_ISCRIZIONE` vale `SOSPESA` (dentro il ramo COMPLETA → non tocca INCOMPLETA/ripresa bozza). Nuovo rollup `MAX_GIORNI_SCADUTO` (MAX di `Giorni dalla Scadenza`) + edit formula su PROD+DEV (manuale, non-API) + supporto UI (badge/label in 4 punti + `getStatoIscrizioneAnnoCorrente` + filtro stato admin). _(Nata EVO-033 → rinumerata: 033 preso dal branch in volo `report-presenze-maestri`.)_ · PR [#91](https://github.com/lucamorettig-coder/trionoracing-next/pull/91) | 2026-07-09 | 2026-07-09 | completata | https://trionoracing.it | [link](evolutive/EVO-034-iscrizione-sospesa.md) |
| EVO-035 | hero-campagne | **Hero homepage dinamica multi-campagna**: comunicazioni hero gestite da Airtable (nuova tabella "Comunicazioni Hero", pattern slot SAFE+ISR + date-range + priorità) con rotazione accessibile in homepage (fallback a hero statica se 0 attive), admin CRUD `/portale/admin/comunicazioni`, e pagina pubblica `/diventa-maestro` (campagna "VOGLIO TE", reclutamento Maestri FCI TI2). 3 comunicazioni seed: Voglio Te, invito iscrizioni, Allenarsi a casa. _(Pianificata come "EVO-033" — rinumerata EVO-035: 033/034 presi da `report-presenze-maestri`/`iscrizione-sospesa`, mergiati su main durante la pianificazione)_ · PR [#93](https://github.com/lucamorettig-coder/trionoracing-next/pull/93) (squash `410c448`) | 2026-07-11 | 2026-07-11 | completata | https://trionoracing.it | [link](evolutive/EVO-035-hero-campagne/EVO-035-hero-campagne.md) |
| EVO-036 | og-default | **OG image di default** (fix `/og/home.jpg` mancante, follow-up EVO-035): nuovo asset branded `public/og/home.jpg` (1200×630, navy + tagline + Nino) + `og:image` di default nel root layout (copre condizioni/contatti/cookie/privacy) + `images` sulle 3 pagine con `openGraph` senza images (chi-siamo/la-scuola/gli-amatori-triono, per il merge shallow Next 16) · PR [#95](https://github.com/lucamorettig-coder/trionoracing-next/pull/95) (squash `d5a2b97`) | 2026-07-11 | 2026-07-11 | completata | https://trionoracing.it | [link](evolutive/EVO-036-og-default/EVO-036-og-default.md) |

## Stati possibili

- **in pianificazione** — fasi 1-6 in corso
- **pronta per implementazione** — fase 7 completata, prompt Claude Code generato
- **in implementazione** — Claude Code sta lavorando
- **in PR** — PR aperta, in attesa di OK utente per il merge
- **deployata** — merge fatto, deploy completato, in attesa di consolidamento
- **completata** — fase 8 chiusa, CLAUDE.md aggiornato
- **bloccata** — ferma per dipendenze
- **annullata** — non procede
- **ombrello** — evolutiva contenitore con sotto-evolutive collegate

## Cronologia narrativa

**2026-07-11 — EVO-036 completata e in produzione (OG image di default)**
Micro-evolutiva chiusa nella stessa sessione, follow-up del bug rilevato in EVO-035 §8: la home referenziava `/og/home.jpg` (in `metadata.openGraph.images` + `LocalBusiness.image` del JSON-LD) ma **il file non esisteva** → condivisioni social senza anteprima. Scelte utente (AskUserQuestion): ambito = home **+ default per tutte** le pagine; immagine = navy + tagline + mascotte. **Deliverable**: (1) nuovo asset `public/og/home.jpg` (1200×630, ~95KB) generato col pattern HTML→Chrome headless→sharp di EVO-035 (card navy + geometrie + eyebrow "Scuola di Ciclismo · Terni" + "In bici, sicuri, insieme." + sottotitolo + Nino col casco; **logo orizzontale scartato** perché invertito in bianco resta un blob confuso); (2) `openGraph.images` di default nel **root layout** → copre le pagine senza `openGraph` proprio (privacy/cookie/condizioni/contatti); (3) `images` ridichiarate su **chi-siamo/la-scuola/gli-amatori-triono** perché — **merge metadata Next 16 shallow** (verificato nei doc bundled) — il loro `openGraph` sovrascrive interamente quello del root. PR [#95](https://github.com/lucamorettig-coder/trionoracing-next/pull/95) squash-merged (`d5a2b97`). Gate verdi. **Verifica**: `og:image` presente su tutte le 10 pagine pubbliche (curl del meta tag), asset 200, `LocalBusiness.image` risolve. 3 pattern nuovi in AGENTS.md (merge shallow Next 16 → default+ridichiarazione, verifica OG via curl del meta tag, logo bianco "smacchione" negli OG generati).

**2026-07-11 — EVO-035 completata e in produzione (hero homepage dinamica multi-campagna + /diventa-maestro)**
Pianificata su Opus via `evolutive-workflow` (fasi 0-6, con fan-out subagenti Sonnet in fase 3/5) ed eseguita in questa sessione. La hero della homepage ora veicola **comunicazioni/campagne gestite da Airtable** invece di un messaggio statico. **Schema** (via MCP, PROD `appszpkU1aXb3xrFM` + DEV `app7FOqBdmmW0jBf5` speculari): nuova tabella "Comunicazioni Hero" (NOME/EYEBROW/TITOLO/SOTTOTITOLO/CTA×2/IMMAGINE_URL/ATTIVA/VALIDO_DA-A/PRIORITA/NOTE) + 3 seed attivi (Voglio Te → /diventa-maestro · Iscrizioni → /portale/iscrizioni · Allenarsi a casa → /la-scuola#allenarsi). **Lib** `src/lib/comunicazioni-hero.ts` (SAFE+ISR 300s, filtro ATTIVA + date-range con `isComunicazioneInCorso` pura, sort PRIORITA), pattern `sfondi-video.ts`. **Hero dinamica**: nuovo componente client `HeroCampagne` (variante A "rotazione" scelta in Fase 6), `ui/hero.tsx` **intoccato**; `HomeHero` fa `Promise.all(sfondo video + comunicazioni)` e fa fallback all'hero statica (stats incluse) se 0 attive. Tutte le slide server-rendered nel DOM (SEO), rotazione ~7s con pausa visibile (SC 2.2.2) + hover/focus/tab pause + reduced-motion, cross-fade animato (grid-stack), barre "altre slide" cliccabili, `h1` unico e statico ("In bici, sicuri, insieme."), TITOLO campagna in `<p>` non-h1. **Pagina** `/diventa-maestro` (manifesto "VOGLIO TE", reclutamento Maestri FCI TI2): hero + Chi cerchiamo + Cos'è la TI2 (**senza claim Giovanissimi non verificato**) + Cosa farai + CTA Contattaci, SEO completa (canonical/OG/sitemap/BreadcrumbJsonLd), OG image dedicata, link nel footer. **Admin CRUD** `/portale/admin/comunicazioni` clonato dal template codici-sconto (EVO-028) con validazioni lunghezze/URL/date/priorità e `revalidatePath("/")` admin→home (primo uso nel repo). **Mascotte** Nino/Vittoria (cutout iwantyou webp): ancorate al bordo inferiore delle sezioni (regola NINO.md §6/§12), ingrandite, verso il centro su schermi larghi, velo navy solo-mobile. PR [#93](https://github.com/lucamorettig-coder/trionoracing-next/pull/93) squash-merged (`410c448`), live. Gate verdi. **Smoke guidato con l'utente** = rete principale: 4 iterazioni recepite sul branch (mascotte ancorate/transizioni/sfondo-geo/padding barre · mascotte più centrali su wide · fix pulsante play che sembrava rotto perché icona derivava da `isPlaying` mascherato da `hoverPaused` su desktop). Verifica screenshot mobile home limitata (Chrome headless non rende il `<video>` a larghezza mobile, lezione NINO.md §12) → confermata dall'utente su device. **Numerazione**: pianificata EVO-033 → **rinumerata 035** (033/034 presi da branch in volo durante la pianificazione — 3ª riconferma lezione EVO-030). `verify-implementation` puntata su "Cycling Experience" → report manuale in `verifica.md`. Azioni residue utente: verifica claim FCI TI2 per riusi social; fix separato `/og/home.jpg` mancante (bug pre-esistente). Pattern nuovi in AGENTS.md.

**2026-07-09 — EVO-034 completata e in produzione (stato iscrizione SOSPESA per pagamenti scaduti)**
Evolutiva piccola cross-cutting (Airtable + supporto UI), pianificata ed eseguita nella stessa sessione via `evolutive-workflow` (planner Opus + executor Sonnet in worktree isolato). Quando un'iscrizione **altrimenti COMPLETA** ha almeno un titolo di pagamento **scaduto da >30gg**, la formula `STATO_ISCRIZIONE` (`TABELLA_ISCRIZIONI`) ora vale **`SOSPESA`**. **Lato Airtable** (manuale UI, PROD `appszpkU1aXb3xrFM` + DEV `app7FOqBdmmW0jBf5`, guidato passo-passo): nuovo rollup `MAX_GIORNI_SCADUTO` = `MAX(values)` su `TITOLI_PAGAMENTO.Giorni dalla Scadenza` + formula estesa con `IF({MAX_GIORNI_SCADUTO} > 30, "SOSPESA", "COMPLETA")` **dentro il ramo COMPLETA** (scelta chiave: non scavalca INCOMPLETA → non rompe la ripresa bozza `getIscrizioneInBozzaPerGenitore` né il resume wizard). Rollup non creabile via API (`create_field` MCP non ha il tipo rollup) e formula esistente non riscrivibile via API → intervento UI necessario. Scelte confermate dall'utente: soglia >30gg, tutti i titoli (non solo rate), SOSPESA solo su ramo COMPLETA. **Lato codice** (PR [#91](https://github.com/lucamorettig-coder/trionoracing-next/pull/91) squash-merged `a4889fd`): supporto al nuovo valore in 4 badge/label (portale + admin, incluso un duplicato locale + un ternary inline), `getStatoIscrizioneAnnoCorrente` (SOSPESA→iscritto), union filtro admin `IscrizioneAdminFilters.stato`, redirect resume wizard `iscrizioni/nuova/page.tsx` (`COMPLETA||SOSPESA`), commento tipo. L'executor Sonnet ha trovato via grep di sicurezza i 2 punti extra (union + resume wizard) non nei 7 edit iniziali. Gate verdi (lint/tsc/build 53/53), prod 200. Verifica interattiva badge = smoke utente (route auth-gated). **Numerazione**: nata EVO-033 → **rinumerata 034** perché 033 era già preso dal branch in volo `evo/EVO-033-report-presenze-maestri` (riconferma lezione EVO-030: verificare i numeri sui branch, non solo su `main`). 3 pattern nuovi in AGENTS.md.

**2026-07-08 — EVO-032 completata e in produzione (descrizione transazioni SumUp con riferimento bambino/causale)**
Evolutiva piccola, chiusa in una sessione via `evolutive-workflow` (pianificazione Opus + esecuzione delegata a executor Sonnet in worktree isolato). Oggi tutte le transazioni SumUp avevano la stessa `description` fissa ("Pagamento iscrizione Triono Racing"), rendendo difficile per l'admin riconoscere a colpo d'occhio nella dashboard SumUp a quale bambino/causale corrispondesse un pagamento. Soluzione: nuovo helper puro `descrizioneCheckoutSumUp(titolo, iscrizione)` in `portale-utils.ts` — compone `"Nome Cognome — causale"` leggendo il nome dai lookup Airtable dell'iscrizione (fallback ai campi piatti, poi fallback alla stringa generica se il nome è assente) e riusando `titoloLabel(titolo).primary` (helper EVO-015) per la causale, con troncamento difensivo a 90 caratteri. Usato in `checkout/route.ts` al posto della stringa hardcoded — **zero fetch Airtable aggiuntivi** (titolo/iscrizione già in memoria per l'ownership check), nessun altro comportamento della route toccato (reference, return_url, idempotenza, sconti invariati). PR [#87](https://github.com/lucamorettig-coder/trionoracing-next/pull/87) squash-merged (`e668ea7`), live su https://trionoracing.it. Quality gate verdi (lint/typecheck/build). **Smoke test E2E reale (login+pagamento) rimandato al preview deploy su decisione esplicita dell'utente** (cambio a basso rischio, stringa isolata con fallback sicuro) — verificato comunque offline: logica pura su 3 casi + runtime della route (compila, risponde 401 senza auth). `verify-implementation` risultata puntata su un altro progetto ("Cycling Experience") → report manuale prodotto (9/9 requisiti ✅, 0 violazioni). 3 pattern nuovi in AGENTS.md (descrizione PSP arricchita zero-fetch, smoke E2E rimandabile per cambi a basso rischio, riconoscere skill puntata su progetto sbagliato).

**2026-06-24 — EVO-031 completata e in produzione (de-index `*.vercel.app` + canonical sul dominio, chiude D-27)**
Cambio piccolo, alto impatto SEO. Google stava indicizzando `https://trionoracing-next.vercel.app` come sito ufficiale perché `SITE_URL` in `src/lib/seo.ts` puntava ancora al deployment Vercel (da lì derivano `metadataBase`→canonical/`og:url`, `robots.ts`→host+sitemap, `sitemap.ts`). DNS cutover **già completato** (apex `trionoracing.it` + `www` live su Vercel). Soluzione: (A) `SITE_URL` → `https://trionoracing.it` (apex) → tutto propaga automaticamente; (B) check host in cima a `clerkMiddleware` (`src/proxy.ts`) — host di **produzione** `*.vercel.app` (`VERCEL_ENV==="production"`) → **308** verso `CANONICAL_HOST` (derivato da `SITE_URL`), path+query preservati; le **preview** (`VERCEL_ENV==="preview"`) **non** sono redirette → restano navigabili per i test. Scelto il **middleware env-gated** invece di `next.config.redirects` per-host (prende tutti gli alias di produzione senza enumerarli). **`www`→apex rimandato** (rischio sessioni Clerk cookie host-scoped; il canonical su apex consolida comunque lato Google). PR [#85](https://github.com/lucamorettig-coder/trionoracing-next/pull/85) squash-merged (`22b558a`), live su https://trionoracing.it. Quality gate verdi. **Smoke prod (curl) tutto ✅** dopo il flip del deploy (vercel.app/ da 200→308): vercel.app→308→apex (path+query preservati), apex 200 con canonical+`og:url` sul dominio, robots/sitemap sul dominio, **preview PR #85 = 401 deploy-protection (non 308 → env-gating confermato)**. Login dominio = smoke utente (Clerk non automatizzabile via curl). Nessuna modifica schema Airtable. **Controlli rischio**: nessun callback hardcoded su `vercel.app` (SumUp `return_url` → Make.com host-independent; grep `src/` pulito). 1 pattern nuovo in AGENTS.md (host-canonicalization redirect env-gated nel middleware). **D-27 chiusa** (riflesso manuale nel PROGETTO_MASTER lato Cowork).

**2026-06-23 — EVO-030 completata e in produzione (profilo genitore obbligatorio)**
Implementata e chiusa in sessione Claude Code. PR [#83](https://github.com/lucamorettig-coder/trionoracing-next/pull/83) squash-merged (`ce10a1a`), live su https://trionoracing-next.vercel.app/portale/iscrizioni/nuova. I record genitore del nuovo portale avevano i campi anagrafici estesi vuoti (solo Nome/Cognome/Email da Clerk) ma servono per il tesseramento FCI del minore. Soluzione: **step condizionale "I tuoi dati"** come Step 01 del wizard iscrizione, mostrato **solo se profilo incompleto** (7→8 step), che salva via PATCH profilo. **Wizard riscritto a sequenza dinamica per chiave** (`StepKey`) invece di indici hardcoded → l'inserimento dello step non rompe gli offset di `computeResume`/`back`/`goNext`/create. **Guard server-side anti-bypass** (422 `PROFILO_INCOMPLETO`) in `POST /api/portale/iscrizioni`. **Banner soft ember** non bloccante in dashboard (`WarningSoftBanner`, nuovo primitivo DS). Pagina profilo resa coerente (campi `required` + validazione CF checksum, tollera omocodia). Helper single source of truth in `portale-utils.ts` + `codice-fiscale.ts`; componente campi condiviso `DatiAnagraficiGenitoreFields`. **Nessuna modifica schema** (campi già presenti). Quality gate verdi (lint/typecheck/build + test checksum CF), smoke utente OK (dev + mobile), deploy prod READY. **Numerazione**: nata "EVO-027" → rinumerata 029 (027/028 occupati) → collisione con `EVO-029-scuola-restyle-v3` (PR #81/#82 in parallelo) → **canonico EVO-030**; codice in history come "EVO-029" (PR #83), docs+commenti allineati a EVO-030.

**2026-06-15 — EVO-029 completata e in produzione (restyle /la-scuola v3)**
Pianificazione + implementazione + chiusura nella stessa sessione (Cowork via `evolutive-workflow`, worktree isolato). PR [#81](https://github.com/lucamorettig-coder/trionoracing-next/pull/81) squash-merged (`3e0407e`), live su https://trionoracing-next.vercel.app/la-scuola. **Corpo pagina ricostruito sul mockup v3** (hero+navbar invariati): Corsi con bolle mascotte, Kit con Vittoria, **2 sezioni nuove** (Allenarsi a casa 4 guide + Sicurezza 5 dotazioni), Galleria mobile → scroll orizzontale, banda iscrizione con duo Nino(certificato)+Vittoria(foto), ordine "parent journey". **Nuova modalità DS `BrandBackdrop`** (forme geometriche brand animate SVG+CSS, **zero JS**, Server Component, reduced-motion/CWV safe; varianti page/cta) — niente three.js. **Asset mascotte Higgsfield** (Nano Banana 2 i2i): nuove scene Allenarsi + duo iscrizione (cutout Adobe via upload CC); **2 regole brand emerse** → mascotte **in sella = casco** + bici con **ruote mag a 5 razze**. **OG image** /la-scuola (fornita dall'utente) → colma il gap "nessuna anteprima social". SEO live verificata (canonical/OG/Twitter/JSON-LD ricco/1 h1/sitemap). Quality gate verdi, smoke utente iterativo (6 feedback recepiti), verifica prod ok. Follow-up: OG image globale per le altre pagine.

**2026-06-13 — EVO-028 completata e in produzione (codici sconto)**
Pianificazione + implementazione diretta + chiusura nella stessa sessione (Cowork via `evolutive-workflow`, **worktree isolato** perché il repo principale era su `feat/sumup-widget-telemetry`). PR #71 squash-merged (`6077d88`), live su https://trionoracing-next.vercel.app. **Codici sconto a importo fisso** applicabili dal genitore al checkout (prima rata iscrizione + titoli/rate) con **validità temporale**, gestiti da nuova area admin `/portale/admin/codici-sconto` (CRUD). Sconto e nuovo importo mostrati in modo evidente; se sconto ≥ importo non applicabile. **Schema Airtable PROD+DEV**: nuova tabella `Codici Sconto` + `TITOLI_PAGAMENTO.IMPORTO_SCONTO_CODICE`/`CODICE_SCONTO` + formula `IMPORTO` estesa (test non distruttivo: blank=0). Sconto **rivalidato server-side** (helper puro `validaCodiceSconto` condiviso preview/checkout, `filterByFormula` injection-safe). UI: `CheckoutSumUp` con step "Procedi al pagamento" (codice prima del widget) + riepilogo barrato/sconto/totale. **Bug latente risolto in smoke**: SumUp non riusa un `checkout_reference` già speso (nemmeno `EXPIRED`) → 409 DUPLICATED_CHECKOUT; fix con **reference univoco** `${baseRef}-${nonce}` mantenendo il `?ref` base per Make.com (caso comune invariato). Verificato via API SumUp diretta. Quality gate verdi, smoke checkout utente ok, verifica prod ok. **7 pattern nuovi in AGENTS.md**. **Residui**: verifica scenario Make.com (`?ref` vs `checkout_reference`, caso raro coperto da `/verify`); **conflitti attesi** con `feat/sumup-widget-telemetry` (stessi 2 file) all'ordine di merge; codici seed DEV rimovibili.

**2026-06-12 — EVO-027 completata e in produzione (3 fix UX portale)**
Pianificazione + implementazione diretta + chiusura nella stessa sessione (Cowork via `evolutive-workflow`). PR #67 squash-merged (`8830b64`), live. Tre fix single-deploy: **#1** dopo creazione figlio → home `/portale?figlio-creato={id}` con banner conferma riusato (`JustCreatedBanner` su `DashboardGenitore`); **#2** restyle header `TariffaCard` + **fix bug latente token** (`var(--grass-500)` inesistente in Tailwind v4, era `--color-grass-500` → gradient vuoto/"slavato" dalla EVO-018) + layout pulito (chip Q, titolo-eroe, meta line, filigrana); **#3** redirect post-login deterministico (`signInFallbackRedirectUrl="/portale"` su `ClerkProvider`) + navbar pubblica auth-aware (`useAuth()`, perché `@clerk/nextjs ^7` Future API **non esporta** `SignedIn/SignedOut`; mantiene statiche le pagine pubbliche). **Residuo #3 (azione utente)**: su Vercel rimuovere/azzerare i legacy `NEXT_PUBLIC_CLERK_AFTER_SIGN_IN/UP_URL` (vuoti = redirect alla landing) + aggiungere i `*_FALLBACK_REDIRECT_URL=/portale` + redeploy. Quality gate verde, smoke dev utente ok, verifica prod ok. 4 pattern nuovi in AGENTS.md. **Nota di processo**: durante la chiusura il repo era passato a `feat/sumup-widget-telemetry` (lavoro parallelo utente) → doc di chiusura prodotti in **worktree isolato** per non interferire.

**2026-06-11 — EVO-026 completata e in produzione (corso "Solo Mountain Bike" + scadenze rate dinamiche)**
Pianificazione + implementazione + chiusura nella stessa sessione (Cowork → Claude Code → docs). PR #64 squash-merged (`faf9794`), live su https://trionoracing-next.vercel.app. **Nuovo prodotto vendibile dal wizard**: secondo tipo di corso **SOLO-MTB** (1 lezione/sett, giovedì; Q1 270€/Q2 180€/Q3 90€, rata 90, iscr 50, kit 125, sconto 20/15/8) accanto a **MTB-BDC**. Il tipo di corso vive su `TABELLA_TARIFFE.TIPO_CORSO` (nuovo singleSelect) e si denormalizza su `TABELLA_ISCRIZIONI.CORSO` alla creazione. **Regola "corso unico" riformulata** (non rimossa): le discipline MTB/BDC restano dei maestri, il tipo di corso è dell'iscrizione via tariffa. **Scadenze rate dinamiche** (regola generale entrambi i corsi): 1ª rata scade nel mese di iscrizione (`createIscrizione` con `MESI_IT_UPPER`), `SCADENZA_RATE` deprecato; rate 2+ restano su Make.com (scenari `4086727`/`5141784`, adeguamento a carico utente al go-live). Wizard a **7 step** (nuovo `StepScegliCorso` 3/7, resume che deriva il corso dal link tariffa via `getTariffaById`). Admin tariffe a 2 sezioni per corso + validazione unicità `(anno, quarter, corso)` con errore inline; filtro/badge corso admin+genitore; vetrina `/la-scuola` "Due formule, una scuola" (niente prezzi) + JSON-LD. Schema applicato via MCP su **PROD+DEV speculari** e verificato. **Scoperte tecniche**: (a) l'Airtable Update Field API NON modifica le choices singleSelect (solo name+desc) → choices aggiunte via `typecast`, vecchie `MTB`/`Strada` lasciate orfane; (b) DEV è duplicato di PROD → stessi table/record ID ma field ID diversi per i campi ri-creati (es. `CORSO`); (c) MCP records richiede field ID non nomi. **Bug pre-esistente fixato in smoke**: navbar "Iscrivi tuo figlio" → `/iscrizioni` (404, nessun redirect) → `/portale/iscrizioni`. Quality gate verdi, smoke utente (preview) + verifica prod ok. 10 pattern nuovi in AGENTS.md.

**2026-06-07 — EVO-024 completata e in produzione (Privacy/Cookie/Condizioni + cookie consent + GA4)**
Pianificazione (Fasi 1-7) in questa sessione via `evolutive-workflow`, implementazione in sessione dedicata (PR #56 `ae1c57e`), chiusura (Fase 8) qui. Live su https://trionoracing-next.vercel.app. Deliverable: 3 documenti legali definitivi in-house GDPR-structured (`/privacy` riscritta, `/cookie` aggiornata, **nuova** `/condizioni` con dati reali titolare A.S.D. CIEMME — P.IVA 01535700551, sede Via Cavour 1 Terni, rappr. Giorgio Roselli, email unificata `trionoracingteam@hotmail.com`), pagine da bozza→`index:true` + sitemap. **Motore di consenso custom nuovo del progetto** (`src/lib/consent.ts` + `src/components/consent/*`): banner non bloccante anti dark-pattern + **Google Consent Mode v2** (default `denied` via `beforeInteractive`, GA4 `G-RMGEYC52J0` montato `afterInteractive` **solo a consenso**), nuovo micro-primitivo `Switch`, gating Google Maps (placeholder click-to-load), stato via `useSyncExternalStore` (no flash SSR, lint-clean). **Bonus**: tabella Airtable `Impostazioni Sito` + `src/lib/site-settings.ts` (SAFE+ISR, pattern EVO-021) → contatti Scuola editabili senza deploy. Testi legali redatti a monte in `content/` (separazione redazione/wiring). Verifica ✅ coerente (nessun ❌), 9/9 criteri, smoke dev+prod ok. **Promemoria utente**: impostare `NEXT_PUBLIC_GA_MEASUREMENT_ID=G-RMGEYC52J0` su Vercel (Prod+Preview) + redeploy per attivare GA (`NEXT_PUBLIC_*` inlined a build-time). 12 pattern nuovi in AGENTS.md. Validazione legale finale resta al titolare.

**2026-06-07 — EVO-023 completata e in produzione (restyle auth + recupero password)**
Workflow `evolutive-workflow` end-to-end in sessione Claude Code (pianificazione + implementazione diretta). Restyle delle 3 pagine auth del portale ispirato ai mockup Claude Design (forniti dall'utente in `evolutive/implementazione Auth/`, copiati come design-handoff). PR #53 squash-merged (`eb39a58`), live su https://trionoracing-next.vercel.app/portale/login (+ registrati + recupero-password). **Decisioni utente (3 domande)**: form login/registrati = Clerk prebuilt + appearance (no form custom); recupero = pagina custom a step; social = solo Google. **Soluzione**: componenti condivisi `AuthSplitLayout`+`AuthBrandPanel`+`AuthHeading` (split-screen navy + pattern.svg + gradient 135°, header compatto mobile) attorno a `<SignIn>`/`<SignUp>`; `clerk-appearance.ts` raffinata a "card nuda" (header nascosto, social bottom, forgot inline nascosto); localizzazione Clerk IT (`@clerk/localizations` `itIT` su ClerkProvider); `proxy.ts` con `/portale/recupero-password` pubblica. **Scoperta tecnica rilevante (nuovo pattern progetto)**: `@clerk/nextjs ^7` usa di default la **Future API** signals-based degli hook — `useSignIn()` ritorna `{ signIn, fetchStatus, errors }` (niente `isLoaded`/`setActive`), i metodi ritornano `{ error: ClerkError | null }` invece di lanciare, e si finalizza la sessione con `signIn.finalize()`. La pagina recupero usa `signIn.create({identifier}) → resetPasswordEmailCode.sendCode() → verifyCode({code}) → submitPassword({password}) → finalize()`. L'MCP Clerk e i tutorial mostrano ancora la legacy API → verificare sempre i tipi installati (`SignInFutureResource`). **Bug latente fixato in smoke**: celle OTP a 6 cifre traboccavano oltre la card perché `<input>` in `flex flex-1` mantengono `min-width:auto` (~20 char) → sostituito con `grid grid-cols-6` (`minmax(0,1fr)`) + `w-full min-w-0`. Quality gate verdi, smoke dev+prod ok. Il Preview MCP è di nuovo finito su `chrome-error://` (pattern EVO-022) → self-check via curl SSR + smoke utente. 4 pattern nuovi in AGENTS.md. **EVO-024 mergiata subito dopo** ha costruito sopra (layout `itIT`+ConsentProvider, recupero `CONTACT_EMAIL`) senza conflitti. **Residuo utente**: abilitare solo Google (no Apple) e opz. Legal consent in Clerk Dashboard.

**2026-06-06 — Kick-off EVO-008 + pianificazione completata (Fasi 0-7)**
Ripresa di EVO-008 (era "in pianificazione" dal 2026-05-21 come ultimo tassello Fase 3 portale, mai avviata). Fasi 0-7 chiuse in sessione. Sintesi:
- **Obiettivo**: trasferire utenti dal portale legacy (Astro + Supabase, repo `/Users/luca/Developer/area-riservata-triono/`) al nuovo portale Next.js + Clerk **senza forzare reset password** — gli utenti entrano con le stesse credenziali (email + bcrypt hash trasferito as-is). I dati applicativi sono già condivisi via la stessa base Airtable, quindi nessuna migrazione dati: il webhook Clerk `user.created` triggerato in cascade dall'import fa il ricollegamento per email-match.
- **Strategia confermata** (4 domande utente): import con password bcrypt (zero touch utente), accesso Supabase completo (service_role), ambito limitato alla sola migrazione utenti (cutover DNS in evolutiva ops successiva), feature "Disabilita account" rinviata da EVO-020 in scope qui.
- **Schema Airtable**: +4 campi additivi su `TABELLA_GENITORI` (`LEGACY_SUPABASE_ID`, `DATA_MIGRAZIONE`, `ACCOUNT_DISABILITATO`, `DATA_DISABILITAZIONE`) PROD + DEV.
- **Script CLI in `scripts/migrate-clerk/`** (nuova convenzione del progetto per script one-shot): `export-supabase-users.ts` (Supabase service_role → JSON gitignored) + `import-clerk-users.ts` (Clerk `createUser` con `passwordHasher: 'bcrypt'` + `passwordDigest` + `publicMetadata.role` letto da Airtable per evitare JWT staleness pattern EVO-016 al primo login + tagging Airtable). Dry-run, idempotenza (skip email già esistenti), rate-limit safe (sleep 100ms Clerk, 200ms Airtable, exponential backoff su 429), report JSON dettagliato.
- **Feature "Disabilita account"** admin: `DisabilitaAccountButton` + `RiabilitaAccountButton` (AlertDialog destructive/confirm, pattern 1:1 `CambiaRuoloModal` EVO-020), Server Action `disabilitaAccountAction` con `clerkClient.users.banUser` + log Airtable. **Decisione: no rollback transazionale** (a differenza di EVO-020 cambio ruolo) perché Clerk è fonte di verità per il blocco — se Airtable PATCH fallisce, log warn e l'admin riprova.
- **Pagina monitoraggio** `/portale/admin/migrazione`: 3 KPI (migrati totali / con utente Clerk creato / mai loggati) + DataTable + filtri + export CSV. Riuso 1:1 pattern admin EVO-016/018.
- **Visual**: skip motivato — riuso 1:1 di pattern esistenti (`CambiaRuoloModal`, `KPICard`, `DataTable`, banner soft). Nessuna invocazione `design:design-system` o Claude Design.
- **Out of scope** confermato: cutover DNS, spegnimento worker legacy `area-riservata-triono`, invio email automatico (template fornito per tool esterno), OAuth provider, audit log dettagliato, bulk disabilita.
- **Prompt Claude Code**: ~14 KB autocontenuto, WBS 17 task in 6 macro, smoke dev 9-step + smoke prod UI + Step I-bis dedicato per esecuzione coordinata degli script con dry-run → pilot 1 utente → full run.
- **Rilevanza**: chiude Fase 3 portale (ultimo tassello pianificato). Sblocca cutover DNS in evolutiva ops successiva. EVO-001 ombrello e EVO-007 ombrello a chiusura di EVO-008 saranno completabili.


PR #37 squash-merged (`2ff3110`), deploy production READY su https://trionoracing-next.vercel.app. Ripresa di EVO-011 (era "in pianificazione" dal 2026-05-23, sbloccata da EVO-010): le dipendenze/coordinamento branch della scheda originale erano già cadute (EVO-004 + EVO-010 entrambe in `main` da settimane), quindi si è lavorato da branch fresco `feat/evo-011-kit-scuola-tab-taglie`. Il tab "Taglie" di `/portale/iscrizioni/[id]` ora mostra le micro foto dei 4 capi del kit (riuso delle Cloudinary di `src/lib/kit-scuola.ts`, le stesse della vetrina `/la-scuola`, rese piccole via `cloudinaryOptimized(url, 200)`) accanto a ogni dropdown; "Taglia tuta" ha le 2 foto felpa+pantalone sopra il select unico (misura condivisa). **Scoperta fuori scope diventata fix principale**: le opzioni delle 3 singleSelect Airtable `TABELLA_ISCRIZIONI` non erano `XS–XXL` come hardcoded nel componente, ma `5XS–4XS–3XS–2XS–XS` (maglia/pantaloncino) e `110/120`, `130/140` (tuta) — salvare i valori vecchi avrebbe prodotto 422 `INVALID_MULTIPLE_CHOICE_OPTIONS`. Fix: nuova const `TAGLIE_PER_CAMPO` in `kit-scuola.ts` (mirror schema Airtable, single source of truth, pattern EVO-005/015) consumata dal componente. Fix design-critique applicati: chevron `ChevronDown` sui select `appearance-none`, `<label htmlFor>` + `alt` reali, gap uniforme, difesa valore legacy. Decisione utente: microcopy "guida taglie" rimosso (nessun tocco a `SezioneKitScuola`). Nota di processo: gli edit erano inizialmente finiti nel repo principale su `main` invece che nel worktree del branch feature → primi quality gate/dev server sul codice non modificato; risolto spostando le modifiche via `git stash` (object store condiviso tra worktree). **Chiude EVO-009 ombrello** (EVO-010 vetrina pubblica + EVO-011 size picker portale ora entrambe completate).

**2026-05-26 (sera) — EVO-020 completata: chiusura scope admin EVO-007 ombrello**
PR #34 squash-merged (`1c64473`), deploy production READY su https://trionoracing-next.vercel.app. Chiusura scope admin post-MVP con 3 nuove aree live: `/portale/admin/lezioni` (storico + KPI + filtri sticky + modal dettaglio + CSV), `/portale/admin/presenze-maestri` (aggregato + drill-down con bulk pagate + 3 modal + 2 CSV), `/portale/admin/genitori` (lista + filtri + dettaglio aggregatore + **modal CambiaRuolo con Server Action transazionale Airtable+Clerk**). **Pattern nuovo del progetto** introdotto: Server Action transazionale con rollback atomico (Airtable autoritativo first → Clerk con timeout 5s → catch fail → PATCH rollback Airtable al ruolo precedente + throw Error esplicito). Verificato lo step (k) dello smoke con un `AUTH_USER_ID` fittizio Clerk-Not-Found → errore inline "Cambio ruolo fallito su Clerk: Not Found. Airtable ripristinato a GENITORE" e rollback eseguito correttamente. Schema PROD+DEV applicato speculare via MCP: TABELLA_MAESTRI +2 campi currency IMPORTO_RIMBORSO_LEZIONE/_GARA, nuova tabella PRESENZE_MAESTRI 9 campi (TIPO singleSelect lezione/gara, MAESTRO link, LEZIONE/GARA link, DATA snapshot, IMPORTO_DOVUTO snapshot, PAGATO checkbox, DATA_PAGAMENTO, NOTE), TABELLA_GENITORI +CREATED_AT (date popolato da `createGenitore` — API Airtable non supporta `createdTime` programmaticamente). Allineamento PROD/DEV preesistente recuperato durante smoke: DEV TABELLA_LEZIONI mancava NOTE_INTERNE (introdotto in EVO-006). Hook PRESENZE_MAESTRI integrato in `createLezione` (EVO-006) e `createGaraAction`/`updateGaraAction` (EVO-019) best-effort non-bloccante + idempotente (check pre-create via inverse field `TABELLA_MAESTRI.PRESENZE_MAESTRI[]`). Iterazioni di hardening post-smoke (4 commit dedicati): allineamento schema DEV NOTE_INTERNE, AlertDialog DS-alignment (centering transform statico + buttonVariants su Action/Cancel), eliminazione flash modali animazione (Tailwind v4 `translate:` vs keyframe `transform:` risolto via `[transform:translate(-50%,-50%)]` arbitrary value), per-row "Segna pagata" action drill-down. Smoke 11/11 OK. 7 nuovi pattern in AGENTS.md (Server Action transazionale, generazione cross-tabella best-effort, inverse field pattern riconferma, cutoff data deploy, Tailwind v4 flash fix, AlertDialog DS-alignment, setState durante render con state-snapshot). **Sblocca chiusura EVO-007 ombrello** (5 sotto-evolutive A-1..A-11 ora tutte completate: EVO-016 A-1 + EVO-017 A-2/3/4 + EVO-018 A-5/A-11 + EVO-019 A-6/A-7 + EVO-020 A-8/A-9/A-10). Azioni manuali residue lato utente: configurare tariffe rimborso sui 9 maestri reali in produzione via modal "Modifica tariffa", eventuale backfill manuale di lezioni storiche importanti via "Aggiungi presenza", comunicazione ai maestri della nuova gestione rimborsi tracciata.

**2026-05-26 — Kick-off EVO-020 + pianificazione completata (Fasi 1-7)**
Avviato workflow `evolutive-workflow` su EVO-020 (ultima sotto-evolutiva ombrello EVO-007 admin). Fasi 0-7 chiuse in sessione Cowork. **Estensione scope significativa**: lo stub originale era "report read-only" su lezioni/presenze/genitori; durante Fase 1 l'utente ha richiesto di aggiungere la **gestione rimborsi spese maestri** (tariffa per maestro per lezione/gara + tracking pagamento per singola presenza + riepilogo dato/da-dare). Decisioni chiuse (2 round AskUserQuestion, 9 domande):
- **Scope**: singolo deploy `feat/evo-020-admin-lezioni-maestri-genitori`, 3 macro-aree + cambio ruolo Clerk-sync transazionale + nuovo modulo rimborsi.
- **Schema Airtable**: 2 modifiche speculari PROD+DEV via MCP: (a) `TABELLA_MAESTRI` +2 campi `IMPORTO_RIMBORSO_LEZIONE`/`IMPORTO_RIMBORSO_GARA` (currency, per-maestro, valore corrente non storico); (b) **nuova tabella `PRESENZE_MAESTRI`** (9 campi: TIPO/LEZIONE/GARA/MAESTRO/DATA/IMPORTO_DOVUTO/PAGATO/DATA_PAGAMENTO/NOTE). Cutoff su data deploy, no backfill — eventi storici via modal "Aggiungi presenza manuale".
- **Generazione presenze**: in codice Next.js (no Make.com). `createLezione` esteso + hook su `createGaraAction`/`updateGaraAction` di EVO-019 (già mergiata `f1ef6c7` quindi zero coordinamento merge). Best-effort non-bloccante + idempotenza pre-create.
- **Cambio ruolo Clerk-sync**: nuovo pattern del progetto **Server Action transazionale con rollback atomico** (Airtable first authoritative → Clerk con timeout 5s → rollback Airtable se Clerk fallisce + errore esplicito in UI). Snippet TS canonico in `evolutive/EVO-020-.../visual/DS-NOTES-evo-020.md` §8.
- **Filtro Corso**: rimosso da A-8 — Triono ha 1 SOLO corso MTB/BDC combinato (regola salvata in memoria persistente Cowork `reference-triono-corso-unico`). _[Superato da EVO-026 (2026-06-11): il **tipo di corso** è ora una dimensione dell'iscrizione (MTB-BDC / SOLO-MTB) via `TARIFFE.TIPO_CORSO`; le discipline restano dei maestri. Filtri/badge Corso reintrodotti in admin. Vedi AGENTS.md §EVO-026.]_
- **Bundle visual**: pattern Cowork-only validato EVO-017/019 — 10 file in `visual/` (README + DS-NOTES con snippet TS Server Action transazionale + 3 F3 reference + 5 mockup MVP nuovi 3170 righe totali coprendo tutte le pagine/modal MVP).
- **Prompt Claude Code**: 16 KB autocontenuto, WBS 6 macro-task/17 task, smoke 11-step incluso step (k) test rollback Clerk fail simulato. Stato → "pronta per implementazione". Effort stimato ~5-6gg (era 3-4gg prima dell'estensione rimborsi).

Out of scope confermati: audit log (EVO-007 ombrello), bulk cambio ruolo (sicurezza), disabilita account (EVO-008), modifica anagrafica admin, notifiche email, tariffe storiche, backfill, PSP per rimborsi, sync Make.com presenze, calendario UI.

**2026-05-21 — Kick-off Fase 3 portale**
Fase 1 (8 pagine statiche) completata. Censimento as-is, UX completa (32 mockup), schema funzionalità per ruolo prodotti in sessioni Cowork precedenti. L'ombrello EVO-001 contiene 7 sotto-evolutive rilasciabili separatamente (EVO-002→EVO-008). Si parte da EVO-002 (infra). Deploy: Vercel collegato a GitHub, auto-deploy su merge su `main`.

**2026-05-22 — EVO-003 completata**
Area genitore core live. Dashboard personalizzata, gestione figli (CRUD + 6 tab con profilo), upload certificato medico e foto su R2, profilo genitore. Nuovi moduli: `src/lib/r2.ts` (client S3/R2), `src/lib/portale-utils.ts` (helper comuni). 5 hotfix post-implementazione principali (FCI mapping, fallbackRedirectUrl Clerk, drawer mobile, suppressHydration footer). Prossima: EVO-004 (iscrizioni + SumUp).

**2026-05-23 — EVO-010 completata (in parallelo a F3, parte di ombrello EVO-009)**
Nuova sezione "Kit Scuola" live su `/la-scuola` tra Filosofia e Maestri. Layout editoriale asimmetrico con 4 capi del kit (maglia, salopette, felpa, pantalone in felpa) + card navy manifesto. Nuovo asset condiviso `src/lib/kit-scuola.ts` (tipo `CapoKit` + array `as const readonly` + helper `cloudinaryOptimized` per trasformazioni URL). Nuovo hostname `res.cloudinary.com/duezeronove/**` in `next.config.ts`. PR #14 squash-merged (commit `72119e1`). Verifica APPROVATA su tutte le 7 dimensioni. 8 nuovi pattern in AGENTS.md (sezione "EVO-010"), tra cui: asset condiviso cross-deliverable, `next/image fill object-contain` per prodotti scontornati, scope ristretto su `images.remotePatterns`, gestione easter egg Claude Design, fallback report verifica quando skill `verify-implementation` non è caricata in sessione. **Sblocca EVO-011** (immagini in `TabTaglie` portale, parte di EVO-009 ombrello). Stato EVO-009 ombrello resta `ombrello` finché EVO-011 non sarà chiusa.

**2026-05-24 — Kick-off EVO-014**
Dashboard genitore portale refactoring: tile colorata stato iscrizione per ogni figlio (grass/ember/sky), Azioni Rapide condizionali, banner reassurance. + 5 bug fix: sync PRIMA_RATA_PAGATA su pagamento, TIPO_TITOLO prima_rata, label "Pagamenti", wizard scegli figlio disabilita già iscritti, CTA lista iscrizioni aggiornata.

**2026-05-24 (sera) — EVO-014 completata + spawn EVO-015**
Dashboard genitore portale completata: tile colorata 3 stati su ogni card figlio (verde grass "Iscritto", ambra ember "Da completare", sky+sun pill "Non iscritto"), banner reassurance verde "Tutti i tuoi figli sono iscritti", Azioni Rapide condizionali (3 voci: Nuova iscrizione condizionale + Iscrizioni + Pagamenti — rimossa "Calendario gare" link rotto), sezione "Prossime scadenze" rifatta aggregando certificati medici + rate non pagate in lista unica con CTA contestuali (rimosso il blocco "Alert urgenti" duplicato). +5 bug fix: sync `PRIMA_RATA_PAGATA` post-pagamento (verify + webhook routes), `TIPO_TITOLO` prima_rata in createIscrizione, label "Piano rate"→"Pagamenti" in sommario wizard, filtro UI wizard per figli già iscritti anno corrente, CTA contestuale lista iscrizioni. PR #20 squash-merged (commit `a73d11f`). Visual Claude Design (HTML standalone 4 artboard) ha guidato l'implementazione con scelte UX rilevanti (sky+sun per "non iscritto" anziché grigio neutro, banner reassurance positivo). Smoke test post-deploy ha rilevato bug "undefinedª rata" su titoli senza NUMERO_RATA → **spawn EVO-015** "Titoli pagamento: campo DESCRIZIONE" con refactor architetturale (Make.com NON popolerà NUMERO_RATA per limite tecnico, soluzione è ridisegnare la label primaria dei titoli usando un campo DESCRIZIONE dedicato). Azioni manuali post-merge in carico all'utente: backfill `PRIMA_RATA_PAGATA` storici + migrazione `TIPO_TITOLO` storici + modifica scenari Make.com 4086727 + 5141784.

**2026-05-24 — Hotfix EVO-004 #17 + spawn EVO-013**
Individuata regressione di EVO-004: il payload `POST /v0.1/checkouts` SumUp non passava `return_url`, disabilitando la notification per-checkout verso Make.com e quindi il fallback "browser chiuso prima del verify". Fix in PR #17 (`fix/sumup-return-url-makecom`, commit `6c0365c`) che reintroduce `return_url` via env `MAKE_SUMUP_RETURN_URL` con spread condizionale e warning non bloccante se assente. Durante il QA della #17 si è notato che il bottone "Vedi pagamenti" sulla dashboard genitore portava a `/portale/iscrizioni` con label fuorviante (non a una vera vetrina pagamenti) — spawnata EVO-013 in parallelo come PR #18 (`feat/portale-pagina-pagamenti`, commit `fa69f67`) con nuova pagina trasversale `/portale/pagamenti`. Merge in ordine #17 → #18, env Vercel `MAKE_SUMUP_RETURN_URL` configurata in production e preview, EVO-004 chiusa (D-17 risolta), EVO-013 chiusa contestualmente.

**2026-05-24 — EVO-012 completata (DS card colorate `.photo-bg-{color}`)**
Scaffold DS: 5 utility CSS nuove in `globals.css` (`.photo-bg-sun|sky|grass|flag|ember`) accanto a `.photo-bg-navy` esistente, formula coerente (bitmap `footer-bg.jpg` per navy, `footer-bg-white.jpg` per gli altri + overlay linear-gradient verticale del colore al 82-90-96%, 88-94-98% per sun/ember chiari per evitare desaturazione, `> * { z-index: 1 }` per children sopra overlay). Migrazione di 8 card navy decorative al pattern `photo-bg-navy` (CtaFinale home, CtaScuola, CtaMarathon, MarathonHero, marathon-209 intro, StepperWizard portale, header DashboardGenitore, sidebar info /contatti). PR #15 squash-merged (commit `cde0230`) + hotfix marathon `58ecc09`. Sezione "Pattern appresi in EVO-012 (2026-05-23)" già in AGENTS.md con 5 pattern (utility scaffold preventivo, when-to-use vs `.pattern-{navy,light}`, override theme-209 solo su navy, migrazione `bg-navy-700` → `navy-900` accettata per coerenza, scaffold preventivo). Riferimento visivo: card manifesto Kit Scuola live su `/la-scuola` (no Claude Design mockup necessario, skip motivato in scheda).

**2026-05-25 (sera) — EVO-016 completata e in produzione**
Prima sotto-evolutiva di EVO-007 ombrello chiusa. PR #29 mergeata, commit `edffe5f`, 38 file + 4673 inserzioni, 96 cancellazioni. Live: https://trionoracing-next.vercel.app/portale/admin. Scaffolding EVO-016 deployato:
- DS primitivi Radix: `Dialog`, `AlertDialog`, `DropdownMenu` in `src/components/ui/`
- 8 componenti admin in `src/components/admin/`: DataTable generico (TS typed, sort + selection + pagination), AdminPageHeader, AdminFilters, BulkActionBar, ConfirmDialog, ExportCSVButton, KPICard, TodayTaskRow
- `src/lib/airtable-admin.ts`: skeleton + 7 wrapper minimal (3 Today's tasks `getCertificatiScaduti`/`getRateScadute`/`getIscrizioniInStallo` + 4 KPI `getKPIIscrizioniAnno`/`getKPIBambiniAttivi`/`getKPIIncassiYTD`/`getKPIPagamentiPending`)
- Schema Airtable: 3 campi nuovi (`ANNULLATA` checkbox, `MOTIVO_ANNULLAMENTO` longtext, `DATA_ANNULLAMENTO` date) + formula `STATO_ISCRIZIONE` estesa con short-circuit `IF({ANNULLATA}, "ANNULLATA", existing)` — applicato su **entrambe le basi PROD `appszpkU1aXb3xrFM` + DEV `app7FOqBdmmW0jBf5`** (recovery DEV applicato da Cowork via MCP dopo che Claude Code aveva fatto solo PROD)
- `portale-utils.ts`: `getStatoIscrizioneAnnoCorrente` + `statoIscrizioneBadge` gestiscono "ANNULLATA" come `non_iscritto` (fix bug latente regressione FiglioCard EVO-014)
- NavBar admin estesa da 4 a 9 link operativi
- Dashboard A-1 minimal live (KPI + 3 Today's tasks + Quick actions + empty state celebrativo ☕)
- 8 placeholder pages "In costruzione (EVO-XXX)" su tutte le sotto-pagine admin
- Route `/api/admin/csv/[entity]` skeleton 501 con auth guard

Smoke 7-step ✅. **2 issue emerse durante smoke**:
- **P1 JWT staleness** (risolto via logout/login): primo accesso `/portale/admin` post-promozione ADMIN → `sessionClaims.role = undefined` perché il JWT Clerk si aggiorna ~60s dopo `syncClerkRole`. Workaround = sign-out + sign-in. Pattern da considerare in EVO future che toccano ruoli Clerk.
- **P2 emoji icons** (fix pre-PR): dashboard passava emoji strings (`"🏥"`/`"💰"`/`"📝"`) a `TodayTaskRow.icon: ReactNode`. Fix = componenti Lucide JSX (`<ShieldAlert/>` ecc.).

4 pattern aggiunti in AGENTS.md: (1) JWT staleness su first admin login, (2) Icone Lucide per `ReactNode` props mai emoji, (3) DEV/PROD schema sync obbligatorio in macro-task 0, (4) `safe()` wrapper per server data fetch resiliente.

**Sblocchi**: EVO-017 (iscrizioni admin), EVO-018 (pagamenti/tariffe), EVO-019 (gare), EVO-020 (lezioni/maestri/genitori) — tutte le 4 sotto-evolutive figlie dell'ombrello EVO-007 sono ora pronte, parallelizzabili su branch indipendenti, ed ereditano lo scaffold completo EVO-016.

**2026-05-26 (sera) — EVO-019 completata e in produzione**
Quarta sotto-evolutiva di EVO-007 ombrello chiusa (admin gare). PR #32 squash-merged (commit `df12f32`), 13 file admin/gare + 2 modifiche additive (`airtable-portale.ts` esposto `mapGara`/`mapIscrizioneGara`/`GARE_TABLE`/`GaraRecord`/`IscrizioneGaraRecord` + nuovo campo `descrizione`/`iscrizioniGareIds`; `airtable-admin.ts` +9 helper gare). Live: https://trionoracing-next.vercel.app/portale/admin/gare. Deliverable:
- **A-6 Gare lista** `/portale/admin/gare`: toggle Future/Passate URL-driven + filtri mese/regione/tipo client-side con **prefiltro mese corrente + Umbria** su Future, search debounced 300ms, DataTable 8 colonne con **tile pill colorato `tipoGara`** (h-8 px-2.5 min-w-44px, no troncamento "STRADA"/"ENDURO"/"ABILITÀ"), counter iscrizioni derivato da `gara.iscrizioniGareIds.length` (no N+1 round-trip), `IN_EVIDENZA` ⭐ icon, kebab azioni `onSelect+router.push` (Apri/Modifica/Iscrizioni — no Elimina duplicato), row click → dettaglio, export CSV 14 colonne.
- **A-6 CRUD manuale**: pagina dedicata `/nuova` e `/[id]/modifica` (no modal), form 4 sezioni con banner ember "caso eccezionale", 12 campi inclusi nuovi **DESCRIZIONE longText user-facing** (visibile a `/portale/gare`) e Note interne admin-only, multi-select maestri attivi chip-style, validation client + Server Action redirect a dettaglio con banner success.
- **A-6 Dettaglio + delete guard**: scheda gara con tile colorato 96x96 + maestri assegnati Badge + CTA "Gestisci iscrizioni N", `EliminaGaraButton` con `ConfirmDialog` destructive + **guard 0 iscrizioni** (Airtable risponderebbe 422 senza guard, gestiamo lato server con `countIscrizioniByGara`).
- **A-7 Workflow iscrizioni gara** `/portale/admin/gare/[id]/iscrizioni`: counter stati Richiesta/Confermata/Rifiutata/Ritirata in alto, filtri chip Stato + search, DataTable 6 colonne con **bottoni inline ✓ grass / ✕ flag** per Approva/Rifiuta (NO kebab — azione primaria), 3 modal `AdminFormDialog` (approva grass / rifiuta flag / bulk dinamico con lista compatta max 5 + "e altri X"), Bulk action bar selezione multipla, toggle "Notifica genitore via email" **inerte MVP** (UI placeholder disabled), `DATA_CONFERMA` auto-valorizzata su Confermata, `bulkUpdateIscrizioniGara` con **batch PATCH 10 record** (limite Airtable). Export CSV 12 colonne con join bambino+genitore+categoria FCI computata.
- **Backend**: `getAllGare` (toggle future/passate via `DATETIME_DIFF({Data},TODAY(),'days')`), `getGaraByIdAdmin`, `getIscrizioniByGara` (join bambino+genitore con `fields[]` mirati), `getAllMaestriAttiviAdmin` (multi-select form), 5 mutation CRUD con `stripGaraReadOnly` whitelist.
- **Schema Airtable**: +1 campo `DESCRIZIONE` (multilineText) su `TABELLA_GARE` applicato **speculare su PROD `appszpkU1aXb3xrFM` (fld `flddz8JWXzCdgtpWi`) + DEV `app7FOqBdmmW0jBf5` (fld `fldEwZxljmBRze05q`)**.

Smoke dev 11-step ✅ con **5 iterazioni di fix in branch** (pattern "smoke rivela bug latenti" continua):
1. **Rate limit 429 Airtable** N+1 `countIscrizioniByGara` → expose `iscrizioniGareIds` su `Gara`, count da `.length` (zero round-trip).
2. **Tile colonna Tipo troncato** → pill rectangular orizzontale auto-width.
3. **Filtri mese/regione/tipo + prefiltro** richiesta utente — replica pattern EVO-005 `FiltriGare` con `key={toggle}` remount per evitare `react-hooks/set-state-in-effect` rule React 19.
4. **`export type` in file `"use server"` rompeva Server Actions** → split in `actions-types.ts` (Next 16 strict: solo async fn export). Root cause del bug "form non submit".
5. **Loop infinito di re-render sui filtri**: `useEffect` con `setParam` (useCallback con `searchParams` in deps) → ogni `router.replace` cambiava searchParams → cambiava ref di setParam → ri-triggerava effect → main thread saturo bloccava click handler dei Link ("Torna al dettaglio non funziona"). Fix: pattern PagamentiFilters (funzione inline + `eslint-disable-next-line` su exhaustive-deps).
6. **NavBar Dashboard sempre attivo su admin** (`pathname.startsWith("/portale/admin")` matchava tutto) → match esatto per route indice (`/portale`, `/portale/admin`), `startsWith(href + "/")` per le altre.
7. **UX**: rimosso kebab Elimina duplicato (azione su dettaglio è sufficiente), kebab DropdownMenuItem da `Link asChild` a `onSelect+router.push` (più affidabile), `onSuccess` callback nei 3 modal con `router.refresh()` per refetch RSC.

Pattern nuovi da consolidare in AGENTS.md (8): type exports in "use server" rompono Server Actions, useEffect deps `setParam` causa loop infinito → pattern inline + eslint-disable, react-hooks/set-state-in-effect risolto via `key={prop}` remount, NavLinks match esatto su route indice, bulk PATCH Airtable batch 10, hard delete con guard count linked, action toggle inline > kebab per flussi primari, expose linked ids `Gara.iscrizioniGareIds` per derivare counter senza N+1.

**Sblocca**: chiude 4/5 sotto-evolutive figlie di EVO-007 ombrello. Resta **EVO-020** (lezioni/presenze maestri/genitori/cambio ruolo Clerk sync) — ultima della famiglia admin.

**2026-05-26 — EVO-018 completata e in produzione + MVP "iscrizioni live" chiuso**
Terza e ultima sotto-evolutiva del MVP `iscrizioni live` (EVO-016+017+018) chiusa. PR #31 squash-merged (commit `28fedcb`), 16 file + 1803 inserzioni / 43 cancellazioni. Live: https://trionoracing-next.vercel.app/portale/admin/pagamenti + https://trionoracing-next.vercel.app/portale/admin/tariffe. Deliverable:
- **A-5 Pagamenti** `/portale/admin/pagamenti`: 3 KPI top (Incassato YTD grass · Da incassare default · Scaduti flag) + filtri sticky multi-dimensione (Anno · Mese · Stato · Metodo · Provider · Tipo · Search debounced 300ms) + DataTable 10 colonne con `MethodTag` colorato (SumUp gradient / sky / neutral / ember) + **Bulk "Segna pagati in blocco"** con riepilogo titoli + totale aggregato + sync hint `PRIMA_RATA_PAGATA` per 1ª rata + dropdown azioni per riga + export CSV 16 colonne contabilità UTF-8 BOM.
- **A-11 Tariffe** `/portale/admin/tariffe`: year selector pills + 3 card Q1/Q2/Q3 con **header gradient** (`grass`/`ember`/`sky`) + `pattern.svg` overlay opacity 0.15 + body breakdown campi semplificati (1 solo `SCONTO_FAMIGLIA_NUMEROSA`) + modal CRUD `AdminFormDialog` con **soft warning ember** se >0 iscrizioni collegate ("modifiche non retroattive") + export CSV 12 colonne.
- **Backend**: `getAllTitoli` (filterByFormula su native/formula fields + in-memory join iscrizione via batch fetch RECORD_ID() OR + search lookup multipli), `getAllTariffe`, `getAnniDisponibiliTariffe`, `countIscrizioniByTariffa`, `parseTitoliFilters`/`parseTariffeFilters` server-safe.
- **Server Actions**: `bulkSegnaPagato` (loop sequenziale rate-limit safe + idempotenza skip già pagati + sync `markPrimaRataPagata` per 1ª rata non bloccante + revalidate per ogni iscrizione coinvolta), `upsertTariffa` con validazione + revalidate.
- **DS extend**: 5 nuovi pattern (`BulkSegnaPagatoModal`, `TariffaCard` header gradient, `WarningSoftBanner` inline ember, `MethodTag` componente DS, `KPICard.valueTone` esteso 4 tone).
- **Zero schema change Airtable**.

Smoke dev 7-step + smoke prod 7-step entrambi ✅. **2 bug fixati nello stesso branch** (pattern "smoke rivela bug latenti" già emerso in EVO-014/017):
1. **Dialog centering** (fix ortogonale cross-feature): `DialogContent` perdeva `translate(-50%, -50%)` alla fine dell'animazione `ds-modal-in` perché il transform era applicato solo nei keyframe → modali drift in basso-destra. Fix: `-translate-x-1/2 -translate-y-1/2` statici nel className. **Corregge tutte le modali EVO-016/017** pre-esistenti.
2. **TIPO_TITOLO enum allineato Airtable** singleSelect (6 valori reali: `prima_rata`, `rata`, `seconda_rata`, `terza_rata`, `Abbigliamento`, `altro`). Filter PagamentiFilters aveva `una_tantum` inesistente + `abbigliamento` minuscolo. AggiungiTitoloManualeModal (EVO-017) aveva 5 enum fantasia (`supplemento_gadget`/`conguaglio`/`sconto_correttivo`/`quota_straordinaria`/`donazione`) che Airtable rifiutava con `INVALID_MULTIPLE_CHOICE_OPTIONS` → fix con valori reali.

5 nuovi pattern in AGENTS.md (sezione EVO-018): bulk modal con riepilogo aggregato, TariffaCard header gradient + pattern (vs `.photo-bg-{color}` editoriale EVO-012), WarningSoftBanner ember non-bloccante, MethodTag componente DS, `KPICard.valueTone` standardizzato 4 tone. + 2 pattern apprendimento "Dialog centering richiede transform statico" e "TIPO_TITOLO enum validation" come hardening cross-feature.

**Sblocca**: chiude MVP "iscrizioni live" (EVO-016 + EVO-017 + EVO-018). Restano sotto-evolutive parallelizzabili dell'ombrello EVO-007 post-MVP: EVO-019 (gare admin), EVO-020 (lezioni/maestri/genitori).

**2026-05-25 — EVO-017 completata e in produzione**
Prima sotto-evolutiva operativa di EVO-007 chiusa. PR #30 squash-merged (commit `6478670`), fix post-merge `f613cf0`, 23 file + 2516 inserzioni. Live: https://trionoracing-next.vercel.app/portale/admin/iscrizioni. Deliverable:
- **A-2 Iscrizioni list** `/portale/admin/iscrizioni`: DataTable 8 colonne (Bambino · Genitore · Corso · Anno · Stato · Modulistica 4-icone · Importo · Azioni) + filtri Anno/Stato multi/Modulistica/Search + export CSV UTF-8 BOM.
- **A-3 Dettaglio iscrizione** `/portale/admin/iscrizioni/[id]`: 5 tab (Stato+override · Modulistica · Taglie · Pagamenti admin · Storia+log) + 4 modal operativi (Annulla · Forza completa · Aggiungi titolo manuale · Segna pagato con sync `PRIMA_RATA_PAGATA`).
- **A-4 Bambini list** `/portale/admin/bambini`: DataTable + filtri Stato cert/Search + colonna "Iscrizione" (anno badge vs contatore) + export CSV.
- **Dettaglio bambino** `/portale/admin/bambini/[id]`: anagrafica + cert + iscrizioni card + EliminaBambinoButton con guard 0 iscrizioni.
- **Schema Airtable**: `NOTE_ADMIN` (multilineText) aggiunto su `TABELLA_ISCRIZIONI` PROD + DEV.
- **7 fix post-smoke**: client boundary (`parseIscrizioniFilters`/`parseBambiniFilters` spostate in `airtable-admin.ts`), max-width dettaglio iscrizione, ModulisticaIcons tooltip nativo, rm filtro MTB/Strada, cert badge "Cert." prefix, card iscrizioni bambino (fallback + quota), anno badge lista bambini.

4 pattern da promuovere in AGENTS.md: parse function server-safe (mai in `"use client"`), join leggero con `fields[]`, Lucide title via `<span>`, `certBadgeVariant` come punto unico di verità.

**Sblocchi**: EVO-018 (pagamenti/tariffe), EVO-019 (gare), EVO-020 (lezioni/maestri/genitori).

**2026-05-25 — Kick-off EVO-007 ombrello + split in 5 sotto-evolutive (EVO-016→020)**
Avviato workflow `evolutive-workflow` su EVO-007 (F3.6 Portale admin). Fasi 0-4 chiuse. Decisioni utente: urgenza alta (2 settimane), CRUD completo da subito, audit log rinviato post-launch, export CSV in scope, cambio ruolo sync Airtable+Clerk publicMetadata, disabilita account rinviato, aggiungi titolo manuale in scope. 3 scoperte chiave Fase 3: (a) Gap DS Dialog/AlertDialog → introdurre in EVO-016; (b) `proxy.ts` ha già guard `isAdminOnly`; (c) `STATO_ISCRIZIONE` è formula autoritativa → soluzione = aggiungere campo `ANNULLATA` checkbox + estendere formula. **Split in 5 sotto-evolutive figlie**: EVO-016 admin-infra-ds (sbloccante, ~3-4gg), EVO-017 iscrizioni+bambini (MVP critico, ~5-6gg), EVO-018 pagamenti+tariffe (~3-4gg), EVO-019 gare (~3-4gg), EVO-020 lezioni+presenze+genitori (~3-4gg). Le 4 sotto-evolutive post-EVO-016 sono parallelizzabili su branch indipendenti. Effort totale 17-22gg; MVP "iscrizioni live" (EVO-016+017+018) realisticamente in 2 settimane, le altre 2 nelle settimane successive. Dashboard A-1 in EVO-016 in versione **minimal** (KPI + Today's tasks + quick actions), trend chart + breakdown corsi rinviati post-MVP. Schema Airtable modificato all'inizio di EVO-016 via MCP. EVO-007 resta ombrello senza visual/code propri — il flusso evolutive-workflow riparte da Fase 1 dedicata su EVO-016 al prossimo kick-off.

**2026-05-24 (notte) — EVO-005 completata (F3.4 calendario gare genitore)**
Vetrina `/portale/gare` + dettaglio `/portale/gare/[id]` + tab "Gare" sulla scheda figlio + rimando dalla dashboard home. Backend: 5 funzioni in `airtable-portale.ts` (`getGareFuture`, `getGaraById`, `getIscrizioniGareByBambino`, `getIscrizioniGareByGenitore` — pattern aggregatore EVO-013, `createIscrizioneGara` con difesa idempotente) + tipi `Gara`/`IscrizioneGara` + costanti `GARA_STATI_ISCRIZIONE`/`GARA_CLASSI` verificate via MCP. Helper `categoriaCompatibile` in `portale-utils.ts` con mapping di gruppo G* → GIOVANISSIMI/GIOCO CICLISMO (permissivo se info mancanti, non bloccante per UX). Server Action `requestIscrizioneGara` con ownership check + idempotenza "Già iscritto" + `revalidatePath` + redirect `?success=N`. Env `AIRTABLE_TABLE_GARE` parametrizzata (default `"Gare Giovanili Umbria 2026"`). 4 componenti gare: `CardGara` (con tile colorato pieno per tipo gara — palette accesa DS: Strada→flag-500, XC→grass-500, Enduro→ember-500, XCC→sky-500, Gioco→sun-500, Abilità→navy-700), `CardIscrizioneGara` (riga compatta), `CardIscriviFigli` (Client multi-select con CTA dinamica), `FiltriGare` (Client con preselezione mese corrente + Umbria + paginazione 5+espandi per gruppo mese). Dashboard home estesa: sezione "Le tue gare" tra "I miei figli" e "Prossime scadenze" se ci sono richieste attive + Quick Action "Calendario gare" con counter. PR #25 squash-merged (commit `fe045a0`).
