<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## Portale — Pattern e convenzioni (F3)

### Client Airtable portale
- File: `src/lib/airtable-portale.ts` (separato da `airtable-209.ts`)
- Pattern: fetch REST API Airtable, nessun SDK. Env: `AIRTABLE_BASE_ID` + `AIRTABLE_TOKEN`.
- Tabella principale: `TABELLA_GENITORI` (field names in MAIUSCOLO_UNDERSCORE).
- Usa sempre `stripReadOnlyFields()` prima di ogni write Airtable (evita 422 su campi formula/lookup).
- Espandi questo file in future EVO aggiungendo funzioni per nuove tabelle.

### Webhook Clerk
- File: `src/app/api/clerk/webhook/route.ts`
- Runtime: nodejs (non Edge) — richiesto da svix.
- Pattern verifica: `new Webhook(secret).verify(rawBody, headers)`
- `user.created`: crea/aggiorna record TABELLA_GENITORI + setta `publicMetadata.role` via clerkClient.

### Middleware (proxy.ts)
- Protegge `/portale/*` eccetto `/portale/login` e `/portale/registrati`.
- RUOLO letto da `auth.sessionClaims?.role` (configurato nel JWT template Clerk Dashboard).
- Fallback se role undefined: tratta come 'GENITORE'.
- Guard admin: `/portale/admin/*` → solo ADMIN. Guard istruttore: `/portale/lezioni/*`, `/portale/gare-assegnate/*` → ISTRUTTORE + ADMIN.

### NavBar portale
- Componente: `src/components/portale/PortaleNavBar.tsx` (Server Component)
- NavLinks: `src/components/portale/NavLinks.tsx` (Client Component — usePathname per active state)
- MobileMenu: `src/components/portale/MobileMenu.tsx` (Client Component — hamburger + dropdown fixed)
- Ruolo letto da `auth()` server-side — mai dal client.
- Link mostrati variano per GENITORE / ISTRUTTORE / ADMIN.

### Struttura route portale
- `src/app/portale/(portal)/` — route group con layout NavBar (protette da auth)
- `src/app/portale/login/` e `src/app/portale/registrati/` — fuori dal gruppo, nessuna NavBar
- `src/app/portale/dashboard/page.tsx` — permanentRedirect a `/portale`
- Il route group `(portal)` non crea segmento URL — `/portale/(portal)/page.tsx` → URL `/portale`

### Deploy
- Vercel collegato a GitHub (lucamorettig-coder/trionoracing-next).
- Branch principale: main. Pattern: branch dedicato → PR → merge → deploy automatico.

### Pattern appresi in EVO-002 (2026-05-21)

- **Lazy sync in layout**: il webhook `user.created` non copre Google OAuth né sessioni pre-esistenti. Il layout `(portal)/layout.tsx` esegue un sync Airtable al primo accesso di ogni utente non ancora collegato — questo è il fallback canonico. Replicare in EVO future se si aggiungono record da creare al primo accesso.
- **Hotfix NavBar pubblica**: quando si aggiunge o modifica il portale auth, verificare che i link "Accedi"/"Registrati" nella NavBar **pubblica** (`src/components/ui/navbar.tsx`) puntino a `/portale/login` e `/portale/registrati`. Aggiungere questo controllo allo smoke test post-deploy.
- **Admin iniziali**: gli account ADMIN esistenti devono avere `publicMetadata.role = "ADMIN"` settato manualmente in Clerk Dashboard → Users → edit user → Public metadata. Il webhook lo setta solo per nuove registrazioni.

### Pattern appresi in EVO-003 (2026-05-22)

- **R2 via AWS SDK S3 (non binding Cloudflare)**: su Vercel non si usano binding Cloudflare nativi. R2 si accede tramite `@aws-sdk/client-s3` con endpoint `https://{CF_ACCOUNT_ID}.r2.cloudflarestorage.com`. Env richieste: `CF_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, `R2_PUBLIC_URL`. Client in `src/lib/r2.ts` — riusare in EVO future senza duplicare.
- **portale-utils.ts**: helper condivisi tra componenti portale (`diffInYears`, `formatDateIT`, `daysUntil`, `certBadgeVariant`). Aggiungere qui ogni utility riusabile cross-componente nel portale, non nei singoli file componente.
- **certBadgeVariant**: usa il campo formula Airtable `CERTIFICATO_MEDICO_STATO` se disponibile, altrimenti calcola da `DATA_SCADENZA_CERTIFICATO`. Pattern generale: preferire campi formula Airtable per stati computati — evita logica duplicata client/server.
- **fallbackRedirectUrl su Clerk SignIn/SignUp**: le pagine `login/` e `registrati/` devono avere `fallbackRedirectUrl="/portale"` sulle componenti Clerk, altrimenti dopo il login Clerk redireziona a un URL di default. Verificare allo smoke test post-deploy.
- **Mobile drawer fuori da sticky header**: il componente mobile drawer/menu deve essere montato **fuori** dall'elemento `<header>` con `position: sticky`, altrimenti si crea un conflitto di stacking context e il drawer non si sovrappone correttamente ai contenuti sotto.
- **suppressHydrationWarning su footer**: gli elementi che contengono data/ora o contenuto che differisce tra server e client render richiedono `suppressHydrationWarning` per evitare warning React. Aggiungerlo su `<footer>` e simili se necessario.
- **allowedDevOrigins per test LAN (iPhone)**: aggiungere `192.168.x.x` (o la subnet locale) a `allowedDevOrigins` in `next.config.ts` per poter aprire il dev server da iPhone sulla stessa rete WiFi. Utile per smoke test mobile.
- **FCI category mapping — verificare sempre**: il mapping anno nascita → categoria FCI aveva un bug nell'implementazione iniziale e ha richiesto un fix separato. Quando si implementa `calcCategoriaFCI`, verificare il mapping rispetto al regolamento FCI vigente e testare con anni limite (es. esatti confini di categoria).

### Pattern appresi in EVO-010 (2026-05-23)

- **Asset condiviso cross-deliverable**: quando un'evolutiva produce contenuti riusati in più punti (sito pubblico + portale, sezione + tab, ecc.), creare un modulo `src/lib/{feature}.ts` con tipo TypeScript + array `as const readonly` + helper di trasformazione URL. Mappa esplicita ai campi backend (es. `CampoTagliaAirtable`). Single source of truth, niente duplicazione tra sezioni. Esempio: `src/lib/kit-scuola.ts` consumato da `SezioneKitScuola` (pubblico) e — in EVO-011 — da `TabTaglie` (portale).
- **`next/image` per immagini prodotto scontornate**: usare `fill` + `object-contain` con padding interno generoso (`p-8 lg:p-10`). NON `object-cover` (è per foto in contesto come `SezioneGalleria`). I capi/prodotti vanno mostrati interi con respiro attorno, non croppati.
- **`images.remotePatterns` scope ristretto**: dichiarare `pathname` specifico al cloud-name del provider (es. `/duezeronove/**` per Cloudinary), non `/**`. Riduce superficie attaccabile e impedisce hot-linking accidentale di asset altrui sullo stesso CDN.
- **Cloudinary trasformazioni inline via helper**: invece di trasformazioni in build, applica `q_auto,f_auto,w_*,c_limit` direttamente nella URL via helper puro (`cloudinaryOptimized(url, width)`). Permette di scegliere width diversi per breakpoint diversi senza ricostruire l'asset.
- **Easter egg da Claude Design vanno puliti**: i mockup Claude Design possono contenere meta-info (es. `EVO-XXX · TITOLO`) come decorazione del canvas. Documentarli in `visual/README.md` come "NON portare in produzione" e verificare la pulizia in fase di verify-implementation.
- **`verify-implementation` skill non sempre disponibile in Claude Code**: la skill citata nei prompt può non essere caricata nella sessione corrente. I prompt devono dire: "se disponibile invoca, altrimenti produci report manuale con la stessa struttura per dimensione". Pattern applicato con successo in EVO-010.
- **Sezioni pagina pubblica: stacco di sfondo**: alternare i background tra sezioni adiacenti per dare ritmo visivo. Pattern usato in `/la-scuola`: Filosofia (`bg-bg-soft pattern-light`) → **Kit Scuola** (bianco pulito, no pattern) → Maestri (default) → Galleria (`bg-bg-soft pattern-light`). Una sezione "pulita" tra due con pattern dà respiro.
- **Lighthouse non in CI**: il progetto oggi non ha Lighthouse automatico. Le metriche restano manuali post-deploy. Eventuale future-task: integrare Lighthouse CI o Vercel Speed Insights.

### Pattern appresi in EVO-012 (2026-05-23)

- **Utility `.photo-bg-{color}` per card decorative grandi**: bg-color + bitmap pattern (`footer-bg.jpg` per navy, `footer-bg-white.jpg` per gli altri) + overlay linear-gradient verticale del colore al 82-90-96% (88-94-98% per sun/ember chiari per evitare desaturazione). `> *` ha `z-index: 1` automatico per portare children sopra l'overlay. Colori disponibili: `navy`, `sun`, `sky`, `grass`, `flag`, `ember`.
- **Quando usare `.photo-bg-{color}` vs `.pattern-{navy,light}`**:
  - **`.photo-bg-{color}`** → card decorative grandi (CTA, manifesto, hero, header dashboard, sidebar contatti). Look "premium" con texture bitmap visibile attraverso l'overlay forte.
  - **`.pattern-navy`** / **`.pattern-light`** → full-section background con SVG geometrico nitido + overlay sfumato (Filosofia UNESCO, PhotoPlaceholder, sezioni testuali). Look "ariosioso" con pattern tile.
- **Override theme-209**: `.theme-209 .photo-bg-navy` cambia bg a red `#7F1D1D` e overlay a red 82-96%. Le utility non-navy (sun/sky/grass/flag/ember) **non hanno override theme** — se in futuro servirà una card colorata sotto theme-209, valutare caso per caso.
- **Migrazione `bg-navy-700 pattern-navy` → `photo-bg-navy`**: la classe `.photo-bg-navy` ha `background-color: #050E3F` (navy-900) hardcoded. Sezioni che usavano `bg-navy-700` cambieranno tonalità (diventano navy-900). Decisione di EVO-012: accettabile per coerenza totale.
- **Scaffold preventivo**: le utility `photo-bg-{sun,sky,grass,flag,ember}` sono aggiunte al DS anche se nessuna card le usa al momento. Pattern: introdurre utility coerenti nel DS quando ne arriva la prima istanza, così le successive non devono pensarci.

### Pattern appresi in EVO-004 — hotfix 2026-05-24

- **`return_url` SumUp = notification URL per-checkout, non redirect del browser**: il campo `return_url` del payload `POST /v0.1/checkouts` (SumUp API v0.1) è l'URL che SumUp chiama in async quando il checkout cambia stato. Va passato ad **ogni** creazione di checkout, e punta al webhook Make.com (env `MAKE_SUMUP_RETURN_URL`) che fa GET su SumUp e aggiorna Airtable (`PAGATO=true`) in modo indipendente dal browser. È il meccanismo di fallback per il caso "utente paga ma chiude il tab prima del `/verify`" — senza, il pagamento resta `PENDING` su Airtable finché qualcuno non riapre la pagina. Pattern ereditato dal legacy Astro (`area-riservata-triono/src/pages/api/pagamenti/sumup/checkout.ts`).
- **Quando si tocca il payload SumUp, includere sempre `return_url`**: già successo — omesso in EVO-004 (PR #13, commit `b37c8e2`) con regressione del fallback Make.com. Spread condizionale `...(returnUrl ? { return_url: returnUrl } : {})` + `console.warn` se la env è assente: degrado, non errore bloccante.
- **Il merchant-level webhook in dashboard SumUp NON è usato**: la notifica è sempre per-checkout via `return_url` nel payload. Non configurare webhook globali in dashboard SumUp pensando che coprano il caso — non lo fanno.
- **Recovery 409 `DUPLICATED_CHECKOUT` non rifà il payload**: il ramo che riusa un checkout esistente (via `GET /v0.1/checkouts?checkout_reference=...`) non deve aggiungere `return_url` — il checkout già esiste lato SumUp con i parametri originali. Modifiche al payload vanno applicate solo al ramo "create".

### Pattern appresi in EVO-004 hotfix + EVO-013 (2026-05-24)

- **`return_url` SumUp è obbligatorio**: il payload `POST /v0.1/checkouts` deve includere `return_url` (env `MAKE_SUMUP_RETURN_URL`) per attivare la notifica per-checkout verso Make.com. Senza, il fallback async "browser chiuso prima del verify" non funziona. Pattern ereditato dal legacy Astro, regressione in EVO-004, ripristinato in PR #17.
- **Spawn evolutiva dal QA**: quando il QA di un fix rivela un gap UI non bug-related (es. pagina/vista assente, label fuorviante), aprire una EVO separata invece di gonfiare il branch del fix. Lascia il fix focalizzato e tracciabile, la feature emerge con sua scheda dedicata. Pattern applicato per #17 (hotfix `return_url`) + #18 (pagina `/portale/pagamenti`, EVO-013).
- **Helper aggregatori cross-iscrizione**: per viste portale che mostrano dati di più iscrizioni del genitore (es. pagamenti, gare, calendario), creare un helper `get{Risorsa}ByGenitore(genitoreId)` in `src/lib/airtable-portale.ts` che fa batch fetch e ritorna anche la mappa `iscrizioneId → iscrizione` per arricchire la UI senza round-trip extra (vedi `getTitoliByGenitore` in EVO-013).

### Pattern appresi in EVO-014 (2026-05-24)

- **Formula `STATO_ISCRIZIONE` Airtable è autoritativa, non modificarla**: richiede 4 condizioni AND (PRIVACY_MINORE + FLAG_REGOLAMENTO + CERTIFICATO_MEDICO_STATO valido + PRIMA_RATA_PAGATA) per ritornare "COMPLETA". Le UI si adattano alla formula, non viceversa. Se un'iscrizione risulta INCOMPLETA inattesa, verificare quale dei 4 campi manca (tipicamente PRIMA_RATA_PAGATA non sincronizzato).
- **Sync TITOLI_PAGAMENTO → ISCRIZIONE per la prima rata**: quando un titolo con `NUMERO_RATA === 1` riceve `PAGATO = true`, settare anche `PRIMA_RATA_PAGATA = true` sull'iscrizione collegata. Pattern in `verify/route.ts` + `webhook/route.ts` post `updateTitoloPagamento()`. Idempotente, non bloccante (warning su fail). Make.com scenario `4086727` PROD + `5141784` DEV devono fare lo stesso update post-EVO-014 (istruzioni manuali nella scheda evolutiva).
- **Helper `getStatoIscrizioneAnnoCorrente(bambinoId, iscrizioni)`** (`portale-utils.ts`): pattern per derivare stato iscrizione per anno solare corrente da una lista di iscrizioni del genitore. Output 3-stati: `iscritto` | `da_completare` | `non_iscritto`. Riusato in dashboard (FiglioCard) e wizard (StepScegliFiglio per disabilitare figli già iscritti). Pattern: helper utility puro stateless > componente intelligente.
- **Helper `buildScadenze(bambini, titoli, iscrizioni)`** (`portale-utils.ts`): pattern aggregatore per viste sintetiche cross-entity. Combina certificati medici (scaduti o ≤30gg) + titoli pagamento non pagati (scaduti o ≤30gg) in lista unica ordinata per urgenza. Sostituisce il duplicato "Alert urgenti" + "Prossime scadenze" della prima implementazione. Riusabile per future viste admin overview.
- **Stati passivi → call-to-action attivi**: il visual Claude Design ha sovrascritto la prima ipotesi UX "non iscritto = grigio neutro" con "non iscritto = sky + sun pill". Pattern: quando il DS ha colori brand che si prestano, lo stato passivo può diventare un richiamo attivo coerente con goal UX di ridurre attriti. Verifica sempre se il "neutro" giustifica la sua presenza.
- **Banner reassurance positivo**: dopo aver mostrato stati negativi/da-completare, mostrare un banner verde "Tutto ok" quando tutti gli stati sono positivi dà chiusura emotiva. Pattern: `bg-grass-50 border border-grass-100` + icona check + "Tutti i tuoi {entity} sono {stato_positivo}". Riusabile in altre aree.
- **Smoke test post-merge rivela bug latenti**: durante il QA EVO-014 è emerso il bug "undefinedª rata" (NUMERO_RATA non popolato dai titoli creati da Make.com). Non in scope originale → parcheggiato in EVO-015. Pattern: split, non gonfiare l'evolutiva corrente.
- **Make.com può avere limiti tecnici**: lo scenario "generazione titolo rata mensile" (id 4746166) non riesce a popolare NUMERO_RATA in modo affidabile. Quando Make.com fa pushback su un dato, non forzare workaround → ripensare lo schema (EVO-015 introdurrà `DESCRIZIONE` come label primaria, smettendo di dipendere da NUMERO_RATA in UI).
- **Quick Actions: rimuovere link rotti quando si tocca l'area**: la voce "Calendario gare" puntava a `/portale/gare` inesistente (rimanda a EVO-005). EVO-014 ne ha approfittato per rimuoverla. Pattern: ogni volta che si refactora un menu/nav, fare audit dei link e bonificare debito tecnico.

### Pattern appresi in EVO-015 (2026-05-24)

- **Campo schema dati come "label primaria umana" quando campi singoli sono inaffidabili**: introdurre un campo dedicato `DESCRIZIONE` (singleLineText) popolato dal punto-creazione (`createIscrizione` per la prima rata, scenario Make.com per rate mensili) invece di costruire la label runtime concatenando campi opzionali. Pattern applicato a `TITOLI_PAGAMENTO` per risolvere "undefinedª rata" da `NUMERO_RATA` non popolato.
- **Helper puro centralizzato per label complesse**: quando ≥3 consumer concatenano campi per costruire una label semantica (es. "Rata 1 · gennaio 2026"), estrarre `titoloLabel(t) → { primary, secondary, secondaryVariant }` in `src/lib/portale-utils.ts` e refactorare i consumer. Elimina mappe locali duplicate e template inline fragili. Pattern applicato a 5 consumer (TabPagamenti, PagamentiLista, StepSommario, DashboardGenitore, checkout).
- **Server Component come "etichetta semantica"**: `<TitoloLabel titolo={t} />` in `src/components/portale/pagamenti/` rende l'output di `titoloLabel()` con styling consistente. Pattern preferibile al template inline quando la label è composta da primary + secondary con variant (es. tipografia diversa, badge accanto).
- **Fallback per tipi sconosciuti in singleSelect**: `TITOLI_PAGAMENTO.TIPO_TITOLO` non è solo `prima_rata`/`rata` — Airtable consente di aggiungere causali manuali (abbigliamento, una tantum). Helper deve gestire default → "Pagamento" invece di assumere enum stretto.
- **Make.com Array Aggregator per evitare duplicati su batch**: quando uno scenario Search Records ritorna N bundle e il flusso a valle creerebbe N record duplicati (es. "genera titolo rata mensile" trova N iscrizioni attive, ma vuoi 1 titolo per iscrizione), inserire un modulo Array Aggregator dopo il Search per collassare i bundle e iterare 1 volta sola.
- **Make.com mapping NUMERO_RATA via aggregator length**: `NUMERO_RATA = length(aggregator.array) + 1` calcola la rata corrente contando quelle pregresse della stessa iscrizione, robusto a re-run dello scenario.
- **Make.com filtro `OR(TIPO_TITOLO=prima_rata, =rata)`**: quando conti rate per determinare il numero progressivo, filtra esplicitamente per i tipi che rappresentano rate vere — escludi causali manuali (abbigliamento, una tantum) che inquinerebbero il conteggio.
- **Schema Airtable additivo è low-risk**: aggiungere campi opzionali a una tabella in produzione è retro-compatibile (record esistenti restano validi con valore vuoto). UI deve sempre avere fallback (`?? 'Pagamento'`) per il periodo di transizione, evitando backfill bloccante.

### Pattern appresi in EVO-005 (2026-05-24)

- **Verificare schema Airtable PRIMA di codare**: vocabolario singleSelect mai dato per scontato. Mockup HTML possono usare label fantasia (`Chiusa`) che non esiste su Airtable PROD (`Ritirata`). MCP `get_table_schema` + sample records via `list_records_for_table` su 3-5 record bastano per acquisire il vocabolario reale. Codificarlo come tipo TS `as const` + helper di mapping (es. `GARA_STATI_ISCRIZIONE`, `GARA_CLASSI`). Pattern già emerso in EVO-015, riconfermato qui.
- **Env table-name parametrizzata anche se hardcodable**: quando una tabella ha nome legato all'anno (es. `Gare Giovanili Umbria 2026`), introdurre subito env `AIRTABLE_TABLE_X` con default hardcoded. Costa zero ora, evita un refactor il prossimo gennaio quando si fa la `2027`. Pattern semplice: `const X_TABLE = process.env.X ?? "default";` + `encodeURIComponent` sull'URL Airtable (i nomi tabella possono contenere spazi).
- **Compatibilità "non bloccante" con warning visivo**: per regole soft (es. compatibilità categoria FCI ↔ classe gara) preferire helper permissivo che restituisce `true` se info mancanti + UI che mostra warning ma non disabilita l'azione. L'utente decide. Più robusto a vocabolari non perfettamente allineati tra entità (Classe gara 2 valori vs Categoria FCI 11 valori).
- **Mockup ricchi su schema povero — adattamento minimal**: i mockup HTML del Cowork possono presupporre campi inesistenti (regolamento PDF, distanza, premi, scadenza iscrizioni). Scelta scope "Opzione A" = no estensione schema, rimuovere gli elementi non supportati dal dato. Documentare gli scostamenti in `evolutive/EVO-XXX/visual/README.md` esplicito (lista "❌ rimosso" + "✅ in scope"). Pattern: il mockup è un riferimento, non un contratto. Annota le scelte di adattamento perché chi rilegge tra 6 mesi capisca *perché* la card "Regolamento PDF" del mockup non c'è in prod.
- **Iterazione UX post-PR sullo stesso branch**: quando il feedback utente arriva DOPO l'apertura della PR ma PRIMA del merge, applicare le modifiche sullo stesso branch (commit incrementale) e riassumerle in PR/scheda evolutiva sezione 8. Non richiede branch nuovo, non aggiunge churn. La PR rimane "una funzionalità coesa" e la sequenza dei commit racconta l'evoluzione. Esempio EVO-005: commit `259339d` aggiunge 5 fix UX (prefilter, sezione in cima, paginazione, tile colorato, rimando home) recepiti dal feedback live tra apertura PR e merge.
- **Tile colorato pieno per categorizzazione visiva (helper centralizzato)**: quando un campo enum (es. tipo gara) ha 4-6 valori distinti e domina il "cosa" della card, una pill colorata piena (`bg-{color}-500 text-white`/`text-navy-900` per giallo) con label corta è più rapida da decodificare di un testo grigio. Centralizzare il mapping in helper puro `tipoXStyle(t) → { bg, text, shortLabel }` con fallback `navy-700` per valori sconosciuti (forward-compat se admin aggiunge nuove voci enum senza aggiornare il codice). Palette DS coerente: flag (rosso, strada/velocità), grass (verde, natura/XC), ember (arancione, avventura), sky (azzurro, breve/veloce), sun (giallo, ludico), navy (default tecnico).
- **Server Action multi-select con difesa idempotente + ownership check**: pattern consolidato (EVO-004/013/015 → EVO-005). `formData.getAll("X_id")` → ownership check via genitore lookup → loop con try/catch su errore-specifico ("Già iscritto") → `revalidatePath` su tutti i path interessati → `redirect(?success=N)`. La difesa "Già iscritto" sta dentro `createX` lato Airtable (non solo in UI) perché la UI può essere bypassata.
- **Section ordering: state attivo > vetrina passiva**: in pagina vetrina con sezioni multiple, ordinare per priorità decisionale dell'utente. "Le tue richieste pending" (state attivo, richiede attenzione) > "In evidenza" (vetrina) > "Lista filtrata" (esplorazione). EVO-005 originariamente aveva richieste in fondo, feedback utente ha invertito. Pattern: chiedersi "se aprissi questa pagina ogni giorno, cosa vorrei vedere subito?".
- **Paginazione per gruppo con state Set espansi (Client)**: per liste raggruppate (es. gare per mese) con N variabile per gruppo, mostrare max 5 per gruppo + bottone "Visualizza tutte (N)" / "Mostra meno" usando `useState<Set<string>>` di chiavi gruppo espanse. Più semplice di un router-state e funziona anche se l'utente filtra (lo state si resetta naturalmente sui cambi filtro? In EVO-005 no — lo state expanded persiste tra filtri perché chiave = mese, non cambia mai). Decisione consapevole, semplice da invertire.
- **Prefilter intelligente basato su data dei record**: invece di filtri statici "Tutti" all'apertura, prefiltrare a valori che probabilmente l'utente vuole vedere. Per `/portale/gare`: mese corrente (se ci sono gare nel mese) + regione "Umbria" (se ci sono gare in Umbria). Calcolato a build-time in `useMemo` su dataset gare. Il bottone "Ripristina filtri" porta ai default prefiltrati, non a "Tutti" — l'utente che vuole vedere tutto deve fare azione esplicita su ogni filtro o "Mostra tutte le gare" nell'empty state.

### Pattern appresi in EVO-006 (2026-05-24)

- **Bug ARRAYJOIN su linked records in filterByFormula — workaround via inverse relationship**: `ARRAYJOIN({CAMPO_LINK},",")` su un campo `multipleRecordLinks` restituisce il **primary field** dei record linkati (formula o singleLineText), NON il loro record ID. Quindi `FIND("recXXX", ARRAYJOIN({CAMPO_LINK},","))` non matcha mai. Manifestazione classica: ricerca cross-tabella tramite linked record fallisce silenziosamente (ritorna sempre 0 record). **Soluzione preferita**: usare l'**inverse relationship** sull'entità che già contiene gli IDs giusti come array. Esempi: invece di cercare lezioni con `FIND("maestroId", ARRAYJOIN({MAESTRO_COMPILATORE},...))`, fetch `TABELLA_MAESTRI/maestroId` e leggi `LEZIONI_COME_COMPILATORE` + `LEZIONI_COME_MAESTRO`. **Soluzione alternativa** (quando l'inverse non esiste): aggiungere un campo lookup `XXX_RECORD_ID_LOOKUP` come fatto in `TABELLA_BAMBINI.GENITORE_RECORD_ID_LOOKUP` e `TABELLA_ISCRIZIONI.GENITORE_RECORD_ID_LOOKUP`. **Soluzione tampone** (quando si ha già il record di partenza in mano): leggi il primary field via record-by-id, deriva l'email/identificativo univoco e fai cross-lookup con quello (es. `getMaestroByGenitoreId` ridisegnata via `EMAIL_GENITORE` match dopo fetch del genitore).
- **Test reali con account autorizzato dopo deploy preview**: il 2026-05-24 il merge di EVO-006 ha rivelato 2 bug consecutivi ARRAYJOIN che erano invisibili in tipi/build/lint (la query non throw, ritorna solo array vuoti). **Pattern**: dopo PR aperta, prima del merge, fare almeno 1 round di smoke test con un account reale che abbia dati linkati. Il typecheck + build sono ortogonali a questa categoria di bug.
- **Lazy sync via tabella anagrafica diversa**: il pattern lazy sync EVO-002 (`syncGenitore` al primo accesso) è estendibile a tabelle "ruolo-specifiche" (es. `TABELLA_MAESTRI`) con la stessa filosofia: cerca per email match → linka via campo collegamento (`UTENTE` in EVO-006) → log warn non bloccante se manca. Costo basso, evita onboarding manuale. **Anti-pattern**: NON aggiungere un secondo `AUTH_USER_ID` duplicato sulla tabella ruolo-specifica — usa il lookup verso il genitore.
- **NavBar dual ruolo derivato dai dati**: per casi "utente con ruolo X che è anche Y" (es. `ISTRUTTORE` con figli iscritti), non normalizzare un array di ruoli o introdurre flag dedicati. Deriva dai dati: `hasFigli = (genitore.TABELLA_BAMBINI?.length ?? 0) > 0`. La logica di rendering della NavBar e della dashboard si rami su quel boolean. Costo zero schema, zero migrazione, e l'utente che cambia stato vede subito la nuova UI senza interventi admin.
- **Card riusabile da altra evolutiva ≠ pagina dettaglio riusabile**: la `CardGara` di EVO-005 è stata replicata in `CardGaraAssegnata` per il maestro (pattern coerente). Ma la **pagina dettaglio** `/portale/gare/[id]` è specifica del flusso genitore (`notFound()` su gare passate + componente `CardIscriviFigli`) e NON va riusata per il maestro: la sua semantica è "voglio iscrivere mio figlio". Per il maestro la card va resa non-clickable o si pianifica una pagina dedicata. Pattern: distinguere "componenti DS" (riusabili tra ruoli) da "pagine workflow" (legate al ruolo target).
- **Quando rimuovere un link è meglio che fare una nuova pagina**: di fronte a un 404 da link in scope ambiguo, valutare prima di tutto se la navigation aggiunge valore reale. Se la pagina di destinazione non esisterebbe nel "flusso ideale" dell'utente target, rimuovere il `<Link>` è la soluzione più honest. Documentare il parking lot (es. "vista dettaglio gara per maestro → EVO-007") e proseguire.

### Pattern appresi in EVO-016 (2026-05-25)

- **JWT staleness su primo accesso admin**: il middleware `proxy.ts` legge `sessionClaims?.role` dal JWT Clerk. Se `syncClerkRole` scrive `publicMetadata.role` nel layout, il JWT vecchio NON riflette il cambio finché non scade (~60s) o l'utente fa sign-out/sign-in. Il middleware gira prima del layout, quindi la prima visita a `/portale/admin` da account appena promosso ADMIN può essere reindirizzata a `/portale`. **Workaround per Luca**: sign-out + sign-in dopo aver configurato l'account ADMIN in Clerk Dashboard. Pattern: notare questo ritardo nello smoke test — non è un bug del codice ma della pipeline JWT Clerk.
- **Icone Lucide nelle prop `icon: ReactNode`**: quando un componente accetta `icon: React.ReactNode`, passare sempre JSX (`<IconName size={20} className="..." />`), mai stringhe emoji. Le emoji non ereditano classi CSS né size — appaiono al posto delle icone senza errori in console. Colori di severity: `text-flag-500` per critical, `text-ember-500` per warning.
- **Airtable DEV/PROD schema sync come macro-task 0**: le modifiche schema Airtable vanno applicate su ENTRAMBE le basi (PROD `appszpkU1aXb3xrFM` e DEV `app7FOqBdmmW0jBf5`) nella stessa sessione MCP. In EVO-016 DEV era rimasto disallineato (3 campi + formula mancanti) e ha richiesto recovery manuale. Pattern: WBS delle EVO future deve includere esplicitamente "DEV schema sync" come sotto-task di macro 0, subito dopo PROD.
- **`airtable-admin.ts` per helper admin-only**: separare nettamente helper portale-admin da `airtable-portale.ts`. Il modulo `src/lib/airtable-admin.ts` contiene KPI aggregati (oggi: 7 funzioni `getKPI*`, `getCertificatiScaduti`, `getRateScadute`, `getIscrizioniInStallo`), `fetchAllPages<T>()` generico, e `buildCSV()`. Le evolutive figlie (EVO-017→020) aggiungono qui i propri wrapper, non in `airtable-portale.ts`.
- **Dashboard admin: `safe()` wrapper per ogni fetch KPI**: ogni chiamata Airtable sulla dashboard admin è wrapped in `safe(fn)` che cattura eccezioni e ritorna `null`. Evita crash totale se una tabella non è accessibile (env mancante, rate limit, preview deploy senza variabili). I KPI card mostrano "—" come fallback. Pattern da replicare in tutte le pagine admin che aggregano dati da più tabelle.

### Pattern appresi in EVO-018 (2026-05-26)

- **Bulk action modal: sempre riepilogo + totale + sync hint per side-effect non ovvi**: quando si introduce un'azione bulk sulla DataTable (es. "Segna pagati in blocco"), la modal deve mostrare lista titoli selezionati (max-height + scroll), totale aggregato in evidenza, e un banner `role="alert"` per ogni side-effect non ovvio (es. "N titoli sono 1ª rata: aggiornerò anche `PRIMA_RATA_PAGATA` sulle iscrizioni collegate"). L'admin deve sapere prima di submit cosa esattamente sta succedendo lato dati. Pattern in `src/components/admin/pagamenti/BulkSegnaPagatoModal.tsx`. Loop sequenziale lato Server Action per rispettare rate limit Airtable (5 req/s) + idempotenza skip per record già nello stato target.
- **TariffaCard header gradient + pattern.svg overlay**: pattern admin per categorizzazione visiva forte (3 card Q1/Q2/Q3 con header colorato + pattern SVG tile). Diverso da `.photo-bg-{color}` editoriale di EVO-012 (gradient SVG + pattern leggero, no bitmap). Implementazione: `linear-gradient(135deg, var(--{color}-500), var(--{color}-600))` + `background-image: url('/assets/pattern.svg'); background-size: 180px 180px; opacity: 0.15` sull'overlay absolute. Children `z-index: 1`. Riusabile per future card admin con identità Q1/Q2/Q3, MTB/Strada, eccetera.
- **WarningSoftBanner ember inline (non-bloccante)**: per warning informativi non-bloccanti (modifiche non retroattive, contesto con conseguenze), pattern `bg-ember-50 border border-ember-100 border-l-[3px] border-l-ember-500 text-ember-700` + icona `<AlertTriangle/>` + `<strong className="text-ink">` per la metrica chiave. Distinto da `<Alert variant="warning">` standard perché l'admin **può procedere**; non chiede conferma. Inline come `<div role="alert" className="…">`, non serve componente dedicato.
- **MethodTag come componente DS**: tag mono uppercase per `METODO_PAGAMENTO` con colore semantico per metodo. SumUp brand (`linear-gradient(135deg, #1c79e6, #00b86b)` + bianco) per `app`; sky-100/700 per `bonifico`; neutral bg-muted/ink per `contanti`; ember-100/700 per `pos_segreteria`; fallback bg-muted/ink-muted per valori sconosciuti. Helper `methodTagVariant(metodo)` esposto per uso da liste dense quando non serve il componente. Riusabile per altre liste/dashboard contabili.
- **`KPICard.valueTone` standardizzato 4 tone**: convention `default | success | warning | critical` mappati a `text-ink | text-grass-700 | text-ember-700 | text-flag-500`. NON lasciare colore al consumer ad-hoc — chiunque crea un KPI sceglie tra le 4 semantiche. Convention per dashboard: positivo (`success`) = incassi/bambini attivi · neutro (`default`) = volumi/conteggi · warning (`warning`) = stalli/iscrizioni incomplete · critico (`critical`) = scaduti/pendenze.
- **Dialog centering richiede transform statico** (hardening cross-feature): il `DialogContent` Radix con `fixed left-1/2 top-1/2` PERDE il transform alla fine dell'animazione di entrata se `translate(-50%, -50%)` è applicato SOLO dentro `@keyframes`. Le animazioni CSS smontano il transform al termine, lasciando la modale derivata in basso-destra. Fix: aggiungere `-translate-x-1/2 -translate-y-1/2` statici al className del Content, **in aggiunta** al transform nei keyframe (l'animazione scale 0.96→1 continua a funzionare). Bug latente in EVO-016 (introduzione Dialog) emerso solo in EVO-018 smoke perché le modali EVO-017 erano piccole e l'offset poco visibile.
- **TIPO_TITOLO singleSelect: enum validation cross-feature**: il singleSelect Airtable `TIPO_TITOLO` ha 6 valori reali (`prima_rata`, `rata`, `seconda_rata`, `terza_rata`, `Abbigliamento`, `altro`, case sensitive). Qualsiasi UI che salva su questo campo deve usare ESATTAMENTE questi valori, altrimenti Airtable risponde `INVALID_MULTIPLE_CHOICE_OPTIONS` (errore 422 ma silenzioso nella UI se non gestito). Pattern: definire il type TS unione di literal **derivato da Airtable** + helper per popolare select. Bug latente EVO-017 (5 enum fantasia tipo `supplemento_gadget`/`conguaglio`/`donazione`) emerso solo in EVO-018 smoke perché l'admin non aveva mai cliccato "Crea titolo manuale" in EVO-017.
- **Smoke test rivela bug latenti cross-feature**: già emerso in EVO-014/017. EVO-018 smoke ha rivelato 2 bug **non in scope EVO-018** ma in EVO-016/017: dialog centering + TIPO_TITOLO enum. Decisione consolidata: fixare nella stessa PR se piccoli (1-3 file) e ortogonali al lavoro corrente; altrimenti spawnare EVO hotfix dedicata. Aggiungere sempre in scheda evolutiva sezione "Iterazioni / bug recepiti durante smoke" perché il pattern ricorre — gli smoke test sono la rete principale contro questa categoria di regressioni.

### Pattern appresi in EVO-019 (2026-05-26)

- **`export type` da file `"use server"` rompe Server Actions in Next 16**: il file con direttiva `"use server"` al top accetta solo **async function exports**. Un `export type X = ...` o anche un `export const helper = ...` non-async causa silenziosamente la mancata registrazione delle Server Action: il form fa `await action(payload)` ma il browser non invia mai la POST RSC. Sintomo: "il bottone non reagisce", nessun errore in console. Pattern: **separare i tipi in `actions-types.ts`** (no direttiva) e importarli via `import type` dentro `actions.ts`. Bug root cause del "form non submit" smoke EVO-019, identificato dopo iterazioni.
- **`useEffect` con `setParam` (useCallback) o `searchParams` in deps causa loop infinito**: pattern del filtro debounced URL-driven. Se `setParam` è `useCallback([..., searchParams])`, ogni `router.replace` cambia `searchParams` → nuovo ref `setParam` → `useEffect` con `[search, setParam]` ri-fires → setTimeout cleanup+re-schedule → loop. Main thread saturo blocca i click handler dei `<Link>` ("Torna al dettaglio non funziona", nessuna GET nel network). **Pattern canonico** (replicato da `PagamentiFilters` EVO-018, validato in produzione): definire `setParam` come **funzione inline non memoized** + `useEffect` con deps `[search]` solo + `// eslint-disable-next-line react-hooks/exhaustive-deps`. La closure cattura i valori al fire del timer — è sufficiente.
- **`react-hooks/set-state-in-effect` (React 19) risolto via `key={prop}` remount**: la regola lint rifiuta `setState` sincrono dentro `useEffect` (causa cascading renders). Quando serve **resettare state derivato da prop che cambia** (es. `setMese(initialMese)` quando toggle Future/Passate cambia), non usare `useEffect`. Pattern canonico: il parent passa `key={prop}` al componente figlio → React rimonta da zero con `useState(initialValue)` fresco. Costa una mount, ma è semanticamente corretto e linter-clean. Applicato a `GareTableWithFilters` con `key={filters.toggle}`.
- **NavLinks active state: match esatto per route indice, startsWith con slash terminale per le altre**: `pathname.startsWith(link.href)` è ambiguo quando due link sono uno prefisso dell'altro (es. `/portale/admin` + `/portale/admin/gare`). Su `/portale/admin/gare/*` entrambi matchavano → Dashboard sempre attivo. Pattern: route indice (`/portale`, `/portale/admin`) → match esatto `pathname === link.href`. Altre voci → `pathname === link.href || pathname.startsWith(link.href + "/")` con slash finale per attivare correttamente le pagine figlie (es. `/[id]/iscrizioni` mantiene attivo "Gare"). Applicato anche su `MobileMenu` per coerenza desktop/mobile.
- **Action toggle inline ✓/✕ batte kebab DropdownMenu per flussi primari**: quando l'azione è il loop critico dell'utente (es. admin approva iscrizioni gara dozzine di volte al giorno), bottoni inline icona-only con colore semantico (`text-grass-700` outlined / `text-flag-500` outlined) sono **più veloci da scansionare e cliccare** rispetto a "click kebab → leggi label → click voce". Riservare il kebab per azioni secondarie (Apri, Modifica, etc). Esempio: `IscrizioniGaraDataTable` colonna "Azioni" 150px con 2 button 32x32 outlined disabled al 30% opacity quando già nello stato target.
- **Bulk PATCH Airtable max 10 record per richiesta**: limite Airtable hard. Pattern per `bulkUpdate*`: loop a batch `BATCH_SIZE = 10`, costruire array `records: [{id, fields}]` per ogni batch, fail-fast su primo errore. Applicato in `bulkUpdateIscrizioniGara` (EVO-019) come prima istanza pulita — riusabile per qualsiasi bulk admin action futura.
- **Hard delete con guard count linked records**: Airtable accetta DELETE su record con linked records ma il comportamento dipende dal tipo di link (preserveOnDelete vs cascadeOnDelete). Per `deleteGara`, evitiamo l'inconsistenza con guard esplicito: prima della DELETE chiamiamo `countIscrizioniByGara(id)` (count da `gara.iscrizioniGareIds.length`), se `>0` ritorniamo `{ok: false, reason: "has_iscrizioni", count}`. UI mostra ConfirmDialog disabled con messaggio. Pattern generale: ogni hard delete su entity con linked records → guard di consistency lato Server Action prima del DELETE.
- **Esporre linked-record IDs sul mapper per derivare counter senza N+1**: pattern emerso fixando il 429 rate limit. Invece di `await Promise.all(items.map(async (i) => count(i.id)))` (1 richiesta per item), aggiungere `entityRecord.fields.LINKED_FIELD` al mapper (`mapGara` espone `iscrizioniGareIds: f.ISCRIZIONI_GARE ?? []`) e calcolare `count = entity.linkedIds.length` lato consumer. Cost zero, no rate limit, no parallel race. Estendere il tipo TS è additivo non-breaking.
- **Bug smoke iterativi tracciati nel branch — convergenza naturale, no scope creep**: EVO-019 ha richiesto 7 commit di fix post-smoke (rate limit / filtri mancanti / use-server type exports / kebab nav / loop infinito / NavBar active / setState-in-effect) tutti applicati sul branch feature pre-merge. Pattern già emerso EVO-014/017/018 — formalizzato qui: lo smoke con utente reale è la **rete principale** contro: (a) regressioni di pattern Next 16 non documentati, (b) leakage da feedback UX implicito non in scope iniziale, (c) bug di rate limit / quote esterne (Airtable 5 req/s), (d) regole lint nuove (React 19) non emerse in dev su altre evolutive. Decisione: continuare a iterare sul branch finché smoke OK, evitando "PR-merge-poi-hotfix" — il blocco di iterazioni cumulate è più honest da revieware.

### Pattern appresi in EVO-020 (2026-05-26)

- **Server Action transazionale Airtable+Clerk con rollback atomico**: pattern **nuovo del progetto**, primo del suo genere. Per qualsiasi mutation che debba mantenere coerenza tra Airtable (DB) e Clerk (auth/role): leggi RUOLO precedente per snapshot → guard early se identificatore Clerk mancante (no rollback necessario, transazione mai iniziata) → PATCH Airtable first (autoritativo) → `Promise.race([clerk.users.updateUserMetadata, timeout(5s)])` → catch Clerk fail → PATCH rollback Airtable a snapshot precedente → throw esplicito con messaggio per UI tramite `useFormState`. In caso di rollback fail interno log critical + throw "disallineati: intervento manuale". Snippet TS canonico in `evolutive/EVO-020-.../visual/DS-NOTES-evo-020.md` §8. Smoke step (k) verificato con `AUTH_USER_ID` Clerk-Not-Found → errore inline + rollback Airtable confermato. Riusabile per future operazioni cross-sistema (es. soft-delete con notifica esterna, dual-write a Stripe/Mailchimp).
- **Generazione automatica record cross-tabella in Server Action esistente con best-effort non-bloccante**: quando una mutation primaria (es. `createLezione`, `createGara`) deve generare N record figli in una tabella collegata (es. `PRESENZE_MAESTRI` per ogni maestro presente), wrappare la generazione in helper dedicato (`generatePresenzeForLezione`, `generatePresenzeForGara`) e chiamarlo **dopo** il POST primario in try/catch outer + try/catch per-record. `console.warn` su fail per non re-throware — la mutation primaria non deve fallire se la generazione secondaria ha problemi. Idempotenza obbligatoria via check pre-create (`getPresenzaMaestroByEvento(maestroId, tipo, eventoId)`) per gestire re-run / re-publish / update di entità esistenti. Pattern applicato in `createLezione` (EVO-006) + hook in `createGaraAction`/`updateGaraAction` (EVO-019). Riusabile per future generazioni cross-tabella in cui il "padre" è autoritativo e il "figlio" è derivato (audit log, contatori, snapshot rate-history, etc.).
- **Inverse field per query "tutti i figli di un padre" su linked records** (riconferma pattern bug ARRAYJOIN EVO-006): per cercare PRESENZE_MAESTRI di un maestro specifico, NON usare `filterByFormula SEARCH("maestroId", ARRAYJOIN({MAESTRO},","))` (restituisce sempre 0 record perché il primary field di MAESTRI è `NOME_MAESTRO`, non l'ID). Strategy: fetch `TABELLA_MAESTRI/{id}` → leggi inverse field `PRESENZE_MAESTRI[]` (auto-popolato da Airtable quando `MAESTRO` è linked dall'altra parte) → batch fetch via `OR(RECORD_ID()=...)`. Stesso pattern di `getLezioniByMaestro` EVO-006. **Sempre esporre l'inverse field nel type TS** (`Maestro.fields.PRESENZE_MAESTRI?: string[]`) per consumer downstream.
- **Cutoff su data deploy come strategia per evitare backfill**: quando si introduce un nuovo tracking automatico cross-tabella (es. PRESENZE_MAESTRI generate ad ogni `createLezione`/`createGara`), i record esistenti pre-deploy non sono tracciati. Anziché fare backfill bulk (rischio: tariffe non-snapshot disponibili, dati mancanti, esecuzione lunga), accettare il cutoff e fornire una modal **"Aggiungi presenza manuale"** lato UI admin per coprire eventi storici importanti caso-per-caso. Documentare esplicitamente la decisione in scheda + UI hint contestuale. Riduce rischio + complessità deploy, sposta carico su utente solo per gli eventi che gli interessano davvero.
- **Tailwind v4 `translate:` vs keyframe `transform:` → flash al primo paint**: Tailwind v4 outputta `-translate-x-1/2 -translate-y-1/2` come proprietà CSS `translate: -50% -50%` separata dal `transform:` (NON come `transform: translate(...)` come faceva v3). Quando un'animazione Radix data-state usa `transform: translate(-50%, -50%) scale(...)` come keyframe, tra il primo paint del Portal e l'avvio dell'animazione il browser renderizza un frame con `transform=none` che sovrascrive temporaneamente il `translate:` statico generando un **flash top-left visibile** prima che la modale "snappi" al centro. Fix: usare arbitrary value `[transform:translate(-50%,-50%)]` invece di `-translate-x-1/2 -translate-y-1/2` su tutti i componenti che usano `ds-modal` (Dialog + AlertDialog). Static e keyframe ora usano la stessa proprietà CSS `transform:` e si fondono senza glitch. **Super-set del fix EVO-018** (che aveva risolto un caso analogo con Tailwind v3 ma è stato rotto al passaggio v4 — il fix funziona ancora come hardening del centering finale ma il flash iniziale richiede l'arbitrary value).
- **AlertDialog DS-alignment con buttonVariants wrappati su Action/Cancel**: di default Radix `AlertDialogPrimitive.Action` e `AlertDialogPrimitive.Cancel` sono esposti come componenti raw senza styling. Per coerenza visuale con tutti gli altri bottoni admin (variants `primary` / `ghost` / `destructive`), wrappare entrambi in `forwardRef` con `buttonVariants({ variant: "primary"/"ghost", size: "sm" })` come default className. Override destructive per scenari critici (es. downgrade ADMIN→non-ADMIN) deve usare `buttonVariants({variant:"destructive"})` invece di raw `bg-flag-500 ...` classnames per garantire border/hover/focus-ring coerenti. Bug latente EVO-016 (introduzione AlertDialog) emerso solo in EVO-020 perché AlertDialog non era mai stato usato in produzione prima.
- **setState durante render con state-snapshot bailout (alternativa a `key={prop}` remount)**: la regola lint React 19 `react-hooks/set-state-in-effect` rifiuta `setState` sincrono dentro `useEffect`. Quando serve **re-inizializzare state derivato da prop che cambia** (es. modal che si apre con un genitore diverso → re-init `nuovoRuolo` da `genitore.fields.RUOLO`), il pattern `key={prop}` remount funziona ma rompe close animations. Alternativa: pattern React ufficiale "Storing information from previous renders" (react.dev) — uno state snapshot della prop + setState durante render con bailout check:
  ```ts
  const [prevKey, setPrevKey] = React.useState<string | null>(null);
  const currentKey = open && genitore ? genitore.id : null;
  if (currentKey !== prevKey) {
    setPrevKey(currentKey);
    if (currentKey && genitore) {
      const targetRuolo = genitore.fields.RUOLO ?? "GENITORE";
      if (nuovoRuolo !== targetRuolo) setNuovoRuolo(targetRuolo);
    }
  }
  ```
  React garantisce che setState sincrono durante render con bailout (gating sull'uguaglianza) non causa loop. **NON usare `useRef` per il prev value** — il lint rule `react-hooks/refs` blocca ref.current access durante render. Sempre `useState` per il snapshot.
- **DEV/PROD schema sync è un debito tecnico ricorrente**: pattern AGENTS.md "macro-task 0 sync schema" già documentato post EVO-016 + EVO-019, ma in EVO-006 il campo `NOTE_INTERNE` su `TABELLA_LEZIONI` era stato applicato solo PROD. EVO-020 ha richiesto creazione lezione su DEV e ha trovato il disallineamento al runtime (`UNKNOWN_FIELD_NAME: NOTE_INTERNE` 422). Pattern consolidato: **prima di ogni mutation cross-tabella in una nuova area, verificare `get_table_schema` su entrambe le basi** anche se la tabella sembra "stabile". Costo trascurabile, evita iterazioni post-smoke per allineamento schema legacy.

### Pattern appresi in EVO-011 (2026-06-06)

- **Opzioni di un `<select>` su campo singleSelect Airtable: SEMPRE mirror dello schema reale, mai hardcode "di buon senso"**: `TabTaglie` aveva `TAGLIE = ["XS","S","M","L","XL","XXL"]` hardcoded, ma le 3 singleSelect reali di `TABELLA_ISCRIZIONI` erano `5XS–4XS–3XS–2XS–XS` (maglia/pantaloncino) e `110/120`,`130/140` (tuta). Un valore fuori dalle choices dà **422 `INVALID_MULTIPLE_CHOICE_OPTIONS`** al PATCH — bug latente invisibile a build/lint/typecheck, emerge solo provando a salvare. Pattern (riconferma EVO-005/015/018): verificare le choices via `get_table_schema` e codificarle come TS const vicino all'asset di dominio. In EVO-011: `TAGLIE_PER_CAMPO: Record<CampoTagliaAirtable, readonly string[]>` in `src/lib/kit-scuola.ts`, con commento "mantenere allineato se l'admin modifica le choices". Aggiungere sempre una **difesa valore legacy** nel render del select (`value && !options.includes(value) ? [...options, value] : options`) così un valore storico non più valido non rende il select vuoto.
- **Thumbnail prodotto piccola accanto a un form control = riuso dell'asset di dominio condiviso, non nuovo asset**: per le micro foto nel size picker si sono riusate le 4 URL Cloudinary di `src/lib/kit-scuola.ts` (le stesse della vetrina pubblica `SezioneKitScuola`), rese piccole via `cloudinaryOptimized(url, 200)` + `next/image` `fill object-contain` su box `bg-bg-soft border-line rounded-[var(--radius-lg)]`. Con `fill`, il padding va sull'`<Image>` (`object-contain p-2`), NON sul wrapper (che con `fill` è ignorato perché l'img è `position:absolute inset-0`). `sizes` esplicito per breakpoint (`"(max-width: 640px) 120px, 84px"`). Single source of truth `lib/{feature}.ts` consumato da pubblico + portale = zero duplicazione foto/mapping (pattern EVO-010 riconfermato).
- **`<select>` con `appearance-none` DEVE avere una chevron esplicita**: rimuovere l'appearance nativa toglie l'indicatore di dropdown → l'utente non capisce che è un menu. Pattern DS: wrapper `relative`, `<select className="... pr-9 appearance-none">`, `<ChevronDown className="pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 w-4 h-4 text-ink-muted" />`. Sempre `<label htmlFor={id}>` + `<select id={id}>` con `id` da `useId()` per l'accessibilità (collegamento label↔control).
- **Worktree ≠ repo principale: gli edit via path assoluto possono finire sul working tree sbagliato**: in sessione su un git worktree (`.claude/worktrees/...`), gli Edit/Write con path assoluto `/Users/.../trionoracing-next/src/...` colpiscono il **repo principale** (checked-out su `main`), NON il worktree del branch feature. Sintomo: `git status` nel worktree è pulito, i quality gate e il dev server girano sul codice non modificato. **Fix pulito senza perdere lavoro**: i worktree condividono l'object store git, quindi `git stash -u` nel repo principale + `git stash pop` nel worktree sposta le modifiche sul branch corretto. **Prevenzione**: prima di editare, verificare con `git rev-parse --show-toplevel` che il path target coincida con il working tree su cui si è creato il branch; oppure creare il branch direttamente nel repo principale. I file `.env.local` non sono condivisi tra worktree (gitignored, per-directory) → vanno copiati per far girare `next dev` nel worktree.
- **Riprendere un'evolutiva vecchia "in pianificazione": rivalidare sempre le dipendenze prima di eseguire**: la scheda EVO-011 (scritta 2026-05-23) parlava di "coordinare con branch `evo-004-portale-iscrizioni`" e attendere il merge di EVO-010. Alla ripresa (2026-06-06) entrambe erano in `main` da settimane: la dipendenza/coordinamento branch era già caduta. Pattern: una scheda di planning datata descrive il mondo di allora — al riavvio, controllare lo stato reale di `main`/PR e aggiornare la scheda (e lo stato in `memory.md`) prima di seguire istruzioni di coordinamento ormai obsolete. Si è lavorato da branch fresco su `main`, non dal branch storico.

### Pattern appresi in EVO-021 (2026-06-06)

- **Sfondi video del sito gestiti da Airtable, slot-based, riusabili**: pattern per dare a una sezione uno sfondo video configurabile dall'admin senza deploy. Tabella `Sfondi Video` (PROD `tblwEP6SAFHpKAn06` / DEV `tblfb46LDpSXbpSmI`) mappa `SLOT` (singleSelect: `home-cta`, `home-hero`, …) → `VIDEO_URL` + `POSTER_URL` Cloudinary + `ATTIVO` (checkbox). Helper `getSfondoVideo(slot)` in `src/lib/sfondi-video.ts` (env base portale `AIRTABLE_BASE_ID`+`AIRTABLE_TOKEN`, `next: { revalidate: 600 }`). **Ricetta per aggiungere video a QUALSIASI slot**: (a) aggiungi opzione a `SLOT` su Airtable, (b) crea record con `VIDEO_URL`/`ATTIVO`, (c) nel componente `const v = await getSfondoVideo("slot"); {v && <VideoBackdrop videoSrc={cloudinaryVideoOptimized(v.videoUrl,1600)} posterSrc={v.posterUrl} />}`. Predisposto per Hero e altre CTA — non duplicare la logica fetch/render.
- **`getSfondoVideo` è SAFE (try/catch → null)**: la home pubblica NON deve crashare se Airtable è giù o le env mancano in preview. Su qualsiasi errore/record assente/`ATTIVO=false` ritorna `null` e il consumer fa fallback allo sfondo statico (`.photo-bg-navy`). Stessa filosofia del `safe()` wrapper di EVO-016 per le dashboard admin.
- **Componente `<VideoBackdrop>` riusabile** (`src/components/ui/video-backdrop.tsx`, client): layer `absolute inset-0` con `<video object-cover autoPlay muted loop playsInline aria-hidden>` + overlay gradient navy (preset `cta`/`hero`/`solid`) + fallback `bg-navy-900` se `videoSrc` assente + pausa su `prefers-reduced-motion`. Estratto dal layer video già presente in `hero.tsx` per condividerlo (CTA, Hero, qualsiasi sezione). Va messo come primo figlio di un contenitore `relative`; il contenuto vero sopra con `z-[1]`.
- **CTA con video ≠ footer flat = stacco visivo**: CTA finale home e footer usavano entrambi `photo-bg-navy` → si fondevano. Soluzione: la CTA mostra un video ambient (overlay navy `0.55→0.88`, più leggero del `0.82→0.96` di `photo-bg-navy`) così il video traspare e si distingue dal footer navy statico. Verifica: nell'HTML prerenderizzato `photo-bg-navy` deve comparire **1 sola volta** (solo footer) quando lo slot CTA è attivo.
- **`cloudinaryVideoOptimized(url, width?)` = gemello video di `cloudinaryOptimized`** (image-only, kit-scuola.ts): inserisce `q_auto,f_auto,w_*,c_limit` su URL `/video/upload/`. Cloudinary serve mp4/webm in base al browser via `f_auto`. No-op se non è una upload Cloudinary.
- **Poster da Cloudinary senza secondo asset**: il poster del video è la stessa URL con estensione `.jpg` + transform `so_3.5` (frame a 3.5s): `…/video/upload/so_3.5/…/home-cta.jpg`. Cloudinary genera il frame on-the-fly — niente upload separato.
- **Upload Cloudinary firmato via curl (no SDK/CLI)**: credenziali in `.env.local` (`CLOUDINARY_API_KEY`/`_API_SECRET`/`NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME`). Signed upload: `sig=sha1("folder=…&public_id=…&timestamp=…"+API_SECRET)` poi `curl -F file=@… -F api_key -F timestamp -F public_id -F folder -F signature` su `api.cloudinary.com/v1_1/{cloud}/video/upload`. La Bash tool è sandboxed (no rete) → serve `dangerouslyDisableSandbox` per l'upload. **Mai scrivere il secret in un file**: `set -a; source .env.local; set +a` e usarlo solo nella sessione shell.
- **Video iPhone `.MOV` sono portrait ruotati**: `ffprobe` riporta `width=3840,height=2160` ma con `rotation=-90` → resa **verticale**. In una banda CTA orizzontale (`object-cover`) si vede solo la **striscia centrale**: scegliere clip con soggetto/azione centrati (di spalle = privacy-friendly per i minori). Ottimizzazione web: `ffmpeg -i in.MOV -an -vf scale=720:-2 -c:v libx264 -crf 30 -preset slow -movflags +faststart -pix_fmt yuv420p out.mp4` (ffmpeg autoruota → output upright). 66MB → ~6MB; sotto un overlay navy 82%+ la qualità bassa è invisibile.
- **Home pubblica diventa ISR aggiungendo un fetch Airtable**: `CtaFinale` async che chiama `getSfondoVideo` + `export const revalidate = 600` su `src/app/(public)/page.tsx` → la route `/` passa da fully-static a `○ Static` con Revalidate 10m (visibile in `npm run build`). L'admin vede il cambio video entro ~10 min senza deploy. Pattern già usato da `/marathon-209`.
- **`Hero` cablato al video con sfondo disaccoppiato dalle stats** (estensione EVO-021): `hero.tsx` ora usa `<VideoBackdrop overlay="hero">` quando riceve `videoSrc` (o `variant="video"`), altrimenti il pattern. Le `stats` ("In numeri") sono indipendenti dallo sfondo — gating cambiato da `variant === "pattern" && stats?.length` a `hasStats = !!stats?.length` → si mostrano sia su pattern sia su video. `HomeHero` è async e fa `getSfondoVideo("home-hero")` passando `videoSrc`/`posterSrc` (fallback al pattern se lo slot non è attivo). **Retro-compatibile**: le altre 4 hero (`la-scuola`, `chi-siamo`, `gli-amatori-triono`, dev) restano `variant="pattern"` senza `videoSrc` → nessun video. Verifica build: contare `<video` nell'HTML prerenderizzato (home = 2: hero + CTA; le pagine che includono `<CtaFinale>` ne hanno 1 = il video CTA condiviso, non un leak della hero).
- **Overlay `hero` rinforzato per leggibilità stats**: il preset `hero` di `<VideoBackdrop>` è stato portato da `0.35→0.85` a `0.50→0.88` perché le stats (testo bianco, in alto a destra) su una clip luminosa (cielo/asfalto) avevano contrasto basso. Lezione: quando si mette testo informativo (non solo titolo in basso) sopra un video, l'overlay top va alzato — il titolo sta nella zona scura in basso (`items-end`), ma stats/eyebrow stanno in alto dove lo scrim è più leggero.

### Pattern appresi in EVO-022 (2026-06-06)

- **ESLint: ignorare `evolutive/**`**: i bundle di handoff Claude Design (in `evolutive/EVO-XXX-*/design-handoff/prototype/`) contengono prototipi `.jsx` di reference (Babel in-browser) che NON sono codice di produzione e rompono `npm run lint` (es. `TrIcon is not defined`, `react-hooks/set-state-in-effect`). Aggiunto `"evolutive/**"` a `globalIgnores` in `eslint.config.mjs`. Pattern: ogni cartella di documentazione/handoff con codice-reference va negli ignore del linter, non trattata come sorgente.
- **Sezione "stepper/connettore numerato" come pattern confinato (non primitiva DS)**: il connettore orizzontale con nodi numerati 01–N + linea gradiente (desktop) e rail verticale (mobile) è net-new nel codebase. Tenuto **dentro** `SezioneComeIscriversi.tsx` senza estrarre una primitiva globale finché non c'è una seconda istanza (regola DS già usata in EVO-012). Riusarlo → solo allora valutare l'estrazione.
- **Mockup illustrati inline `aria-hidden` invece di screenshot reali**: per illustrare schermate del portale su pagina pubblica senza esporre dati reali di minori, costruire "disegni" della UI con frame finestra (chrome + url fittizia generica) + barre `navy-100` al posto dei campi + **un solo** pulsante pieno `navy-700` + accento `sun`. Tutto il blocco mock è `aria-hidden` (decorativo). Privacy-safe, themeable, zero asset esterni. Vedi `MockRegister/MockIscrizione/MockCheckout` in `SezioneComeIscriversi.tsx`.
- **Scala colore `sun` del DS è incompleta**: esistono solo `sun-50/100/500/600/700` (mancano 200/300/400). Per un valore intermedio (es. bordo card "invito" `#F2E89A`, tra sun-100 e sun-500) usare un arbitrary hex (`border-[#F2E89A]`), NON assumere `sun-200`/`sun-300`/`sun-400` (la classe non matcha → bordo a `currentColor`, bug silenzioso). In generale verificare la scala reale in `globals.css @theme` prima di usare un grado intermedio.
- **`.reveal` è già reduced-motion safe e scroll-driven** (`animation-timeline: view()` + `@media (prefers-reduced-motion: reduce) { animation: none }` in `globals.css`): per le entrate di sezione preferirlo a un client component con IntersectionObserver. Lo stato base è lo stato finale visibile → niente flash, niente JS, resta Server Component. `reveal-delay-1..6` per lo stagger. I prototipi Claude Design possono proporre sistemi d'animazione custom (IO + play/done): mapparli su `.reveal` per coerenza e semplicità; ambient/hover restano best-effort.
- **Worktree: setup per dev/build/lint**: i worktree (`.claude/worktrees/...`) NON hanno `node_modules` né `.env.local`. Per girare i comandi: `ln -s <repo-principale>/node_modules node_modules` (gli object store git sono condivisi, il symlink risolve) + `cp <repo-principale>/.env.local .env.local`. Build con rete abilitata (fetch Airtable ISR) → usare `dangerouslyDisableSandbox`. Verificare sempre `git rev-parse --show-toplevel` prima di editare (gli edit via path assoluto possono colpire il repo principale).
- **Preview MCP (`Claude_Preview`) può bloccarsi su `chrome-error://`**: se il browser interno carica la pagina prima che il dev server sia caldo, finisce su una error page e NON rinaviga via `location.href` (le error page Chrome bloccano la navigazione JS). `preview_stop`+`preview_start` non sempre recupera. Fallback affidabile per la verifica: `curl` dell'HTML SSR (grep di contenuti chiave, ordine, href dei link) + smoke test visivo dell'utente sul dev server. Non bloccare il ciclo sul self-check via screenshot.
- **Doppia CTA complementare sulla stessa pagina pubblica**: `/la-scuola` ha ora la CTA "iscrizione self-serve" (`SezioneComeIscriversi` → `/portale/iscrizioni`) **e** le CTA "assistite" preesistenti (hero/`CtaScuola` → `/contatti?motivo=scuola`). Convivono perché rappresentano azioni diverse (iscriviti da solo vs prenota una prova / chiedi info). Il link "prova" dello step 01 è volutamente a bassa enfasi (testo + freccia, non bottone pieno) per non competere con la CTA primaria gialla.

### Pattern appresi in EVO-023 (2026-06-07)

- **Clerk "Future API" è il default degli hook in `@clerk/nextjs ^7`**: `useSignIn()`/`useSignUp()` NON ritornano più `{ isLoaded, signIn, setActive }` (legacy) ma la forma signals-based `{ signIn, fetchStatus, errors }`. Il `signIn` è un `SignInFutureResource` con metodi che **ritornano `{ error: ClerkError | null }` invece di lanciare** (niente try/catch obbligatorio; il throw resta solo per errori imprevisti/rete). Flusso reset password: `signIn.create({ identifier }) → signIn.resetPasswordEmailCode.sendCode() → .verifyCode({ code }) → .submitPassword({ password }) → signIn.finalize()` (`finalize()` sostituisce `setActive`). Messaggio errore: `error.longMessage ?? error.message` (`longMessage` è localizzato dalla `localization` del provider). La legacy resta disponibile via `@clerk/react/legacy`. **L'MCP Clerk e i tutorial mostrano ancora la legacy** → prima di scrivere un flusso custom, verificare i tipi installati (`grep` su `SignInFutureResource` in `node_modules/@clerk/types`). I componenti prebuilt `<SignIn>`/`<SignUp>` sono invariati e funzionano comunque.
- **Split-screen auth = wrapper custom + Clerk prebuilt "card nuda"**: `src/components/portale/auth/` con `AuthSplitLayout` (split desktop / stack mobile), `AuthBrandPanel` (navy-900 + `pattern.svg` opacity 0.65 + overlay `linear-gradient(135deg, …)` su un layer `opacity-65` unico, logo "T-mark" sun, feature-pills solo desktop, header compatto su mobile) e `AuthHeading` (eyebrow mono + H2 caldo + sub). Il form Clerk va dentro l'area destra reso "nudo" via appearance: `cardBox`/`card` `bg-transparent shadow-none border-0 p-0`, `header: "hidden"` + `logoBox: "hidden"` (si renderizza `AuthHeading` proprio), `socialButtonsPlacement: "bottom"`. La pagina ha già un H1 nel pannello → l'heading dell'area form è `<h2>`.
- **Nascondere il forgot-password inline di `<SignIn>`**: chiave appearance `formFieldAction__forgotPassword: "hidden"` (verificata nei tipi `@clerk/types`). Usata quando il recupero password ha una **pagina dedicata** (`/portale/recupero-password`) e non si vuole il flusso reset inline del prebuilt. Le chiavi appearance Clerk si verificano con `grep` sui d.ts di `@clerk/types` (es. `header`, `cardBox`, `formFieldAction__forgotPassword`, `otpCodeFieldInput`, `socialButtonsBlockButton`).
- **Localizzazione Clerk IT**: `@clerk/localizations` (export `itIT`) + `localization={itIT}` sul `ClerkProvider` del root layout → tutta la UI Clerk (prebuilt + messaggi d'errore `longMessage`) in italiano, app-wide. Default Clerk è inglese.
- **Nuova route auth pubblica → aggiungerla a `proxy.ts`**: il middleware protegge `/portale/*` con eccezioni in `isPortalePublic` (`createRouteMatcher`). Ogni pagina auth non autenticata in più (es. `/portale/recupero-password`) va aggiunta lì, altrimenti l'utente non loggato viene rimbalzato al login.
- **Celle uguali (OTP/PIN) → grid, non flex**: 6 `<input>` in `flex flex-1` traboccano il contenitore perché gli input mantengono `min-width: auto` (~larghezza di `size` default). Usare `grid grid-cols-6 gap-2` (Tailwind `grid-cols-N` = `repeat(N, minmax(0,1fr))`, che permette lo shrink) + input `w-full min-w-0`. Pattern generale per il pitfall flexbox `min-width:auto`.
- **Mockup "form gestito da Clerk" = ricreare il wrapper, non il form**: quando il mockup Claude Design dichiara nelle annotazioni che il form è prebuilt Clerk, l'HTML è un'**approssimazione** del rendering Clerk. Fedeltà attesa ~90%: il brand panel/wrapper è 100% fedele (sotto nostro controllo), il form segue il rendering del prebuilt (regole password live, layout campi, divider). Non rincorrere il pixel-perfect del form: è una scelta consapevole prebuilt-vs-custom (custom solo dove serve un flusso dedicato, es. lo stepper di reset).

### Pattern appresi in EVO-024 (2026-06-07)

- **Motore di consenso cookie + Google Consent Mode v2 (pattern nuovo del progetto)**: il consenso vive nel **root layout** (`src/app/layout.tsx`, globale su pubblico + portale). Sequenza obbligata: (1) inline `next/script` `strategy="beforeInteractive"` che definisce `window.dataLayer`/`gtag` e chiama `gtag('consent','default', {...'denied'..., wait_for_update:500})` **prima** di GA (in App Router il `beforeInteractive` va nel root layout, finisce in `<head>`); (2) `GoogleAnalytics` (`next/script` `afterInteractive`) montato **solo** se `analytics === granted` **e** env id presente; (3) al cambio scelta `gtag('consent','update', {...})`. Logica pura in `src/lib/consent.ts` (no `"use client"`), componenti in `src/components/consent/`. Riusare integralmente per qualsiasi futuro tracker (Meta/Ads): aggiungere una categoria, non reinventare il motore.
- **Stato consenso letto con `useSyncExternalStore`** (no `useEffect`+`setState`): legge il cookie `tr_consent` come external store → niente flash SSR e **lint-clean** rispetto a `react-hooks/set-state-in-effect` (React 19). Pattern preferito per qualsiasi stato derivato da cookie/localStorage condiviso tra componenti.
- **Cookie consenso `tr_consent` versionato + a scadenza**: JSON `{categorie, v, ts}`, `SameSite=Lax`, `max-age` 6 mesi. `readConsent()` ritorna `null` (→ ri-mostra banner) se assente, versione diversa (`CONSENT_VERSION`), o più vecchio di 180gg. Bump della versione = re-prompt a tutti dopo modifiche rilevanti alla policy.
- **GDPR anti dark-pattern (EDPB/Garante) nel consenso**: "Accetta" e "Rifiuta" devono avere **pari prominenza** (due bottoni pieni, non Accetta-grande/Rifiuta-linkino), e ciò vale **anche nel modal preferenze** (lì "Salva preferenze" è il primario, "Rifiuta tutti"/"Accetta tutti" pari peso outline). X/Escape/click-overlay del modal = **cancel** (nessun consenso implicito). Riga categoria interamente cliccabile per target ampio. Banner **non bloccante**: il sito resta navigabile, gli script gated semplicemente non partono.
- **Gating di embed di terze parti (Google Maps) con placeholder click-to-load**: l'iframe Maps non si carica finché manca il consenso "mappe" → segnaposto brandizzato "Carica la mappa" che, al click, concede il consenso e monta l'iframe. Verifica SSR: 0 `<iframe>` pre-consenso. Stesso schema applicabile a qualsiasi embed terzo (YouTube, social).
- **`NEXT_PUBLIC_*` è inlined a build-time**: una env pubblica usata come "interruttore" (es. `NEXT_PUBLIC_GA_MEASUREMENT_ID`) deve essere impostata su Vercel (**Production + Preview**) **prima del build**; cambiarla dopo richiede un **redeploy**. Se assente, il codice deve degradare in modo sicuro (GA non si carica). Documentare in `.env.local.example` + ricordare il redeploy nella checklist di go-live.
- **`Switch` toggle come nuovo micro-primitivo DS** (`src/components/consent/Switch.tsx`): track 44×24 `rounded-pill`, ON `grass-500`, OFF `navy-200`, disabled `opacity-55`, knob bianco 20px, `role="switch"` + `aria-checked`, focus-ring `navy-700/20`. Nessun nuovo token. Promuovere a `components/ui/` se servirà una seconda istanza (regola DS EVO-012/022).
- **Dati legali centralizzati in `src/lib/seo.ts`**: costante `LEGAL` (ragione sociale, P.IVA, C.F., sede, PEC, rappresentante) + `CONTACT_EMAIL` come single source of truth, consumate da documenti legali + footer + JSON-LD. Cambiare l'email del titolare = **un solo punto** (propaga a Organization/LocalBusiness). L'email pubblica del sito è `trionoracingteam@hotmail.com` (in EVO-024 `info@trionoracing.it` è stata dismessa ovunque).
- **Tabella Airtable `Impostazioni Sito` (key→value) + `src/lib/site-settings.ts`**: contatti/info del sito (es. `scuola-telefono`, `scuola-referente`) **editabili senza deploy**. Helper SAFE (try/catch → fallback) + ISR (`next: { revalidate }`), stesso pattern di `sfondi-video.ts` (EVO-021) e del `safe()` wrapper admin (EVO-016). Modifiche visibili in produzione entro la finestra ISR (~5-10 min). Schema su **PROD + DEV**. Estendere qui ogni futura info di contatto/configurazione gestibile dall'utente.
- **Pagine legali da "bozza" a definitive**: per pubblicare documenti legali rimuovere il banner "Bozza tecnica" e `robots: { index: false }` → `index:true` + `canonical` + `BreadcrumbJsonLd` + voce in `sitemap.ts` (priority bassa 0.3, `yearly`). I testi in-house sono accurati e GDPR-structured **ma la validazione legale finale resta responsabilità del titolare** (annotato in scheda). Per processor USA dichiarare base trasferimento **EU-US Data Privacy Framework** e/o SCC.
- **Redazione testi legali a monte come asset riusabile**: in `evolutive/EVO-XXX/content/*.md` i 3 documenti sono stati redatti nella fase di planning (alta qualità, dati reali) e il prompt Claude Code li ha solo "wired" nelle pagine mantenendo il pattern `Section`/`DataBlock`. Pattern: per deliverable content-heavy, separare *redazione* (planning) da *implementazione* (wiring) riduce ambiguità e alza la qualità.
- **Nota ambiente**: `verify-implementation` non sempre caricata in sessione → report manuale con la stessa struttura per dimensione (riconferma EVO-010, salvato in `verifica.md`). `@clerk/localizations` può mancare da `node_modules` → `npm install` prima del `typecheck`.

### Pattern appresi in EVO-026 (2026-06-11)

- **Regola "corso unico" RIFORMULATA (non più valida così com'era)**: la regola storica "Triono ha 1 SOLO corso MTB/BDC combinato" (memoria Cowork `reference-triono-corso-unico`, filtri Corso rimossi in EVO-017/020) è **superata** da EVO-026. Nuova formulazione: le **discipline** (MTB/BDC) restano attributo dei **maestri** e delle lezioni (`TIPO_SESSIONE`); il **tipo di corso** è una nuova dimensione dell'**iscrizione**, con 2 valori `MTB-BDC` (Strada+MTB, 2 lezioni/sett) e `SOLO-MTB` (giovedì, 1 lezione/sett). Vive su `TABELLA_TARIFFE.TIPO_CORSO` (la tariffa scelta determina il corso) ed è **denormalizzato** su `TABELLA_ISCRIZIONI.CORSO` alla creazione. I filtri/badge "Corso" reintrodotti in admin si riferiscono al **tipo di corso**, non alle discipline. Helper `corsoLabel`/`corsoBadgeVariant` in `portale-utils.ts` (default difensivo MTB-BDC per legacy `MTB`/`Strada`/vuoto).
- **Airtable Update Field API NON modifica le choices di un singleSelect** (solo `name` + `description`): `update_field` MCP e `PATCH /meta/.../fields/{id}` con `options.choices` → **422 INVALID_REQUEST_UNKNOWN**. Per **aggiungere** una choice: scrivere un record con quel valore e `typecast: true` (Airtable la crea on-the-fly). Per **rinominare/rimuovere** una choice: non c'è via API → intervento manuale nella UI Airtable. In EVO-026 le vecchie choices `CORSO` = `MTB`/`Strada` sono rimaste orfane (aggiunte `MTB-BDC`/`SOLO-MTB` via typecast su una scrittura transitoria poi revertita); rimozione manuale opzionale, nessun impatto.
- **PROD e DEV condividono table/record ID ma NON i field ID dei campi ri-creati dopo la duplicazione**: DEV (`app7FOqBdmmW0jBf5`) è un duplicato di PROD (`appszpkU1aXb3xrFM`) → stessi `tbl…`/`rec…` ID e stessi field ID per i campi originari, MA un campo **ri-aggiunto** indipendentemente in ciascuna base ha ID diversi (es. `ISCRIZIONI.CORSO`: PROD `fldsDc2e6lHrFmHzA` vs DEV `fldmlL3EJxXJ8kdgX`). Quando si scrive su un campo del genere via MCP, mappare il field ID **per-base**. I 3 record TARIFFE 2026 (`rec5LoeWCCgEMz5ql`…) erano invece identici su entrambe.
- **MCP Airtable `create/update_records` richiede field ID (regex `fld…`), NON i nomi** (diverso dalla REST API che accetta i field name). Recuperare i field ID via `list_tables_for_base`/`get_table_schema` e costruire il payload con gli ID. (`create_field`/`update_field` usano comunque i field ID.)
- **Scadenze rate dinamiche dal mese di iscrizione (regola generale, entrambi i corsi)**: la 1ª rata in `createIscrizione` usa `SCADENZA_MESE = MESI_IT_UPPER[mese da DATA_ISCRIZIONE]` + `computeDataScadenzaRata` (ultimo giorno del mese). Il vecchio `SCADENZA_RATE` (campo TARIFFE + formula ISCRIZIONI) è **legacy deprecato**: non più letto né scritto dal codice (grep pulito eccetto type-decl con `@deprecated`). Le **rate 2+** (ogni 2 mesi) restano generate da **Make.com** (scenari `4086727` PROD + `5141784` DEV) — l'adeguamento degli scenari alle scadenze dinamiche è **a carico utente, contestuale al go-live**.
- **Inserire uno step a metà wizard = shiftare resume + create + guard**: aggiungere lo step "Corso" (3/7) ha richiesto di aggiornare in `WizardNuovaIscrizione`: `computeResumeStep` (5/6/7), il branch CREATE (ora `step === 4`), il back-guard (`iscrizione && step <= 5`), `isNextDisabled`, e l'array `STEPS` (lo `StepperWizard` deriva il resto). Su cambio corso (solo in fase di creazione) `setTariffa(null)` per forzare il refetch della tariffa col nuovo corso.
- **Resume bozza: derivare la dimensione dal LINK, non ricalcolarla**: con N tariffe per quarter (una per corso) il resume deve leggere `iscrizione.fields.TABELLA_TARIFFE[0]` via `getTariffaById` per recuperare il `TIPO_CORSO` scelto; `calcTariffa(corso-derivato)` serve **solo** per lo sconto famiglia. Ricalcolare con default `MTB-BDC` perderebbe la scelta dell'utente. Pattern generale per ogni dimensione "scelta e congelata" su un record.
- **`AdminFormDialog` hardening — catch su `onSubmit`**: aggiunto `try/catch` che, su fallimento, **non chiude** il dialog e logga (niente unhandled rejection / overlay dev). Abilita gli **errori inline** nei form: il form fa il proprio `try { await action } catch { setErrore(msg); throw }` — il re-throw gating la chiusura, il catch di `AdminFormDialog` evita il crash. Usato per la validazione unicità tariffa `(anno, quarter, corso)` in `upsertTariffa`.
- **`git add -A` trascina untracked/cruft nella feature branch**: un export locale (`Design System Triono/`) e la modifica di planning a `memory.md` sono finiti nei commit feature via `git add -A`. Preferire `git add <path>` espliciti; gitignorare gli export locali; tenere `memory.md`/`AGENTS.md`/scheda-§8 **fuori dalla feature PR** (vanno nella docs PR). Lo squash merge ripulisce comunque il net diff (`main...HEAD`), ma la history feature resta sporca.
- **Navbar pubblica "Iscrivi tuo figlio" → `/portale/iscrizioni`** (riconferma EVO-002): puntava a `/iscrizioni`, route inesistente (nessun redirect in `next.config`) → 404 silenzioso, emerso solo nello smoke. Quando si tocca il funnel iscrizione, fare audit dei link navbar pubblica (desktop + mobile) verso le route portale.

### Pattern appresi in EVO-027 (2026-06-12)

- **Tailwind v4: i token colore sono `var(--color-*)`, NON `var(--*)`**. I custom token definiti in `@theme` (`--color-grass-500`) sono esposti **solo** come `--color-grass-500`. Un `var(--grass-500)` inline (senza prefisso `color-`) risolve a **vuoto** → fallback silenzioso: `linear-gradient(135deg, , )` = gradient assente → header "slavato" (era il bug di `TariffaCard` fin da EVO-018). Le utility (`bg-grass-500`, `text-ember-700`) funzionano comunque; solo i `var()` scritti a mano negli `style={{}}` sbagliano. Audit: `grep -rEn "var\(--(grass|ember|sky|navy|sun|flag)-[0-9]" src` → tutti devono avere il prefisso `--color-`.
- **Clerk redirect post-login: niente env legacy `AFTER_SIGN_IN/UP_URL`**. I deprecati `NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL`/`…AFTER_SIGN_UP_URL`, se **vuoti** (`""`), risolvono a `/` e **scavalcano** il `fallbackRedirectUrl` del componente → dopo login si atterra sulla landing. Bug visibile **solo in PROD** (in `.env.local` erano `/portale/dashboard` → mascherato). Fix canonico: `signInFallbackRedirectUrl`/`signUpFallbackRedirectUrl="/portale"` su `ClerkProvider` + env moderni `NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL`. Usare **`fallbackRedirectUrl`** (rispetta il `redirect_url` → deep-link return da pagina protetta), **mai `forceRedirectUrl`** (lo romperebbe). `NEXT_PUBLIC_*` inlined a build-time → modifica su **Vercel** richiede **redeploy** (pattern EVO-024). Segnale che la var è su Vercel: compare in `.env.production.local` (pull `# Created by Vercel CLI`).
- **`@clerk/nextjs` ^7 (Future API) NON esporta `<SignedIn>/<SignedOut>`**. La entry principale espone control component `ClerkLoaded/ClerkLoading/Show/RedirectTo*` e gli hook (`useAuth`, `useUser`, …, re-esportati da `@clerk/react`), ma `SignedIn`/`SignedOut` vivono **solo** lato server (`protect`). Per auth-state lato client: **`useAuth()`** (`{ isLoaded, isSignedIn }`) o il control `Show`. **`useAuth()` in un client component mantiene STATICHE le pagine pubbliche** (stato risolto client-side post-hydration; un `auth()` server-side nel layout pubblico le renderebbe tutte dinamiche). Default a "signed-out" finché `isSignedIn === undefined` (load) → nessun flash per i visitatori anonimi. Usato per la navbar pubblica auth-aware ("Vai al portale" se loggato).
- **Redirect post-azione → home con conferma riusata**: per "torna alla home dopo aver creato X": `router.push('/portale?figlio-creato={id}')` (client) → la home (Server Component) legge `searchParams` (Promise in Next 16), trova l'entità nei dati **già caricati** (no fetch extra) e rende il banner esistente (`JustCreatedBanner`) passato come prop a `DashboardGenitore`. Riuso del DS, zero duplicazione di fetch.

### Pattern appresi in EVO-028 (2026-06-13)

- **SumUp NON riusa un `checkout_reference` già speso** (nemmeno se il vecchio checkout è `EXPIRED`/terminale) → `409 DUPLICATED_CHECKOUT`. Quindi un titolo con `checkout_reference` fisso (`CODICE_TITOLO`) può avere **un solo checkout nella vita**; appena l'importo cambia (es. codice sconto applicato dopo un tentativo precedente) serve un **reference nuovo**. Pattern: **separare il `checkout_reference`** (chiave SumUp, può diventare `${baseRef}-${nonce}`) dal **`?ref` del return_url** (resta sempre il `CODICE_TITOLO` base, per il match Make.com). Tentare prima col `baseRef` (caso comune invariato); su 409, riusare il checkout attivo se ha lo stesso importo, altrimenti disattivarlo e ricreare con reference univoco. Mai bloccare l'utente. **⚠️ Verificato post-merge — Make NON usa il `?ref`**: lo scenario Make `4086727` (PROD) / `5141784` (DEV) trova il titolo con la formula Search `{CODICE_TITOLO} = "{{2.data.checkout_reference}}"`, cioè dal **reference REALE del checkout** (ottenuto via `GET /v0.1/checkouts/{id}`), non dal `?ref` del return_url. Quindi col reference univoco il fallback Make **non aggancia** il titolo finché la formula non viene estesa a `OR({CODICE_TITOLO}="{{2.data.checkout_reference}}", {CODICE_TITOLO}="{{last(split(2.data.return_url; "ref="))}}")` (il `return_url` restituito da SumUp contiene `?ref=CODICE_TITOLO`, da cui si estrae il base). Senza la fix resta scoperto **solo** il caso "reference univoco + browser chiuso prima del `/verify`"; il `/verify` browser copre il flusso normale. Lo scenario Make si ispeziona via MCP `scenarios_get` (blueprint) — l'`executions_get-detail` NON espone i payload dei moduli. **Bug latente pre-esistente** (un checkout scaduto bloccava il reference) emerso con EVO-028 perché lo sconto cambia l'importo. Diagnosticabile via API SumUp diretta: `set -a; source .env.local; set +a` poi `curl -H "Authorization: Bearer $SUMUP_API_KEY" "https://api.sumup.com/v0.1/checkouts?checkout_reference=..."` (status del checkout) e `DELETE /v0.1/checkouts/{id}` (disattiva un PENDING → 200).
- **Sconto su `IMPORTO` (formula) via campo dedicato, non riuso del campo esistente**: la formula `TITOLI_PAGAMENTO.IMPORTO` è `(RATA_BASE+ISCRIZIONE)−IMPORTO_SCONTO_APPLICATO` e `IMPORTO_SCONTO_APPLICATO` è **già** lo sconto famiglia. Per il codice sconto si aggiunge un campo writable dedicato (`IMPORTO_SCONTO_CODICE`) e si **estende la formula** sottraendolo, così TUTTA la UI che legge `IMPORTO` (lista, dashboard, admin, CSV, checkout) mostra automaticamente lo scontato. **Blank di un currency in formula aritmetica = 0** in Airtable: verifica non distruttiva → dopo l'estensione, rileggere alcuni record esistenti (campo nuovo vuoto) e confermare gli `IMPORTO` invariati su **PROD e DEV**.
- **Input utente in `filterByFormula` = injection-safe per costruzione**: Airtable non ha parametri bind. Normalizzare l'input (`trim().toUpperCase().replace(/[^A-Z0-9_-]/g,"")`) prima di interpolarlo, e usare `UPPER({CAMPO})="VALORE"` per il match case-insensitive. La **stessa funzione di normalizzazione** in scrittura (admin) e lettura (lookup) garantisce che il match funzioni sempre.
- **Helper di validazione puro condiviso tra preview e commit autoritativo**: una sola funzione pura (`validaCodiceSconto(record, importo, oggi)`, in `src/lib/{feature}.ts`) usata sia dalla **preview** (server action, nessuna scrittura, con ownership-check titolo→genitore) sia dalla **rivalidazione** nella route che esegue il pagamento. Il client mostra solo l'anteprima; l'importo viene **sempre ricalcolato server-side** al checkout — mai fidarsi del client.
- **Widget di pagamento che fissa l'importo alla creazione → step esplicito prima del mount**: il Card Widget SumUp blocca l'`amount` quando si crea il checkout. Per consentire modifiche pre-pagamento (applicare/rimuovere un codice), NON creare il checkout al mount del componente: aggiungere uno stato `started` ("Procedi al pagamento") e gateare su di esso l'`useEffect` che crea il checkout. Il riepilogo + campo codice stanno prima; il widget appare dopo il click.
- **Route protette Clerk → `404` (non redirect) per non autenticati**: in smoke prod via `curl` una route auth-gated risponde `404` per non rivelarne l'esistenza. Non è "route mancante": confrontare con una route esistente (es. `/portale/admin/tariffe` → anch'essa `404`) e usare `next build` come fonte di verità sull'esistenza della route. La verifica **funzionale** delle route auth richiede login (smoke utente), non è automatizzabile via curl.
- **Worktree isolato quando c'è lavoro parallelo sugli stessi file**: la sessione girava in `.claude/worktrees/...` (branch `evo-028`) mentre il repo principale era su `feat/sumup-widget-telemetry` — che tocca gli **stessi** file (`CheckoutSumUp.tsx`, `…/sumup/checkout/route.ts`). L'utente che guarda la cartella principale **non vede** il codice del worktree (branch diverso) → spiegarlo. Annotare i **conflitti attesi** nella PR e decidere l'ordine di merge. Setup worktree: symlink `node_modules` + copia `.env.local` (non condivisi); `git add` **espliciti** in commit (il symlink `node_modules` resta untracked, non va committato).

### Pattern appresi in EVO-029 (2026-06-15)

- **Nuova modalità sfondo DS `BrandBackdrop`** (`src/components/ui/brand-backdrop.tsx`): forme geometriche brand fluttuanti, reimplementazione **leggera del backdrop three.js** di un mockup → **SVG inline + animazioni CSS keyframe, ZERO JS** → resta **Server Component**. Si animano solo `transform`/`opacity` (GPU): niente CLS, INP trascurabile, animazioni auto-throttlate fuori viewport. Varianti `page` (layer ambient, navy bassa opacità) e `cta` (wireframe + accenti sky/sun per card scure). reduced-motion → forme ferme; `.bd-m` nascoste su mobile. **NO three.js** su landing conversion-critical (~150kb). **Parallax rimosso** (lezione `NINO.md` §12). Spec in `evolutive/EVO-029-.../visual/DS-NOTES-brand-backdrop.md`.
- **`.brand-backdrop--page { position: fixed }` (CSS unlayered) per battere `.fixed` di Tailwind**: in Tailwind v4 le utility stanno in `@layer`, quindi una regola CSS **unlayered** in `globals.css` ha priorità maggiore di una utility. Per dare `position:fixed` alla variante page senza override via className, definirla in una regola unlayered dedicata. Pattern: quando una utility Tailwind e una regola custom unlayered confliggono, conta il **layering**, non solo la specificità.
- **Scene mascotte su sfondo bianco SENZA cutout per i banner card**: le immagini generate su PURE WHITE si fondono sulla tinta della card con **`mix-blend-multiply`** (come le foto kit, EVO-010) → niente cutout Adobe per scene su card chiare. Il **cutout (Adobe `image_remove_background`) serve solo per fondo scuro/navy** (es. banda iscrizione). Risparmia step.
- **Adobe `image_remove_background` — CloudFront Higgsfield non whitelisted**: l'URL `*.cloudfront.net` di Higgsfield è rifiutato. Flusso (NINO.md §3): scarica PNG → `asset_initialize_file_upload` → `PUT` byte sulla transfer URL (`curl -L -X PUT --data-binary @file`) → `asset_finalize_file_upload` (transfer document verbatim) → `image_remove_background` con la `presignedAssetUrl` (`at.adobe.com`, whitelisted) → output `photoshop-api.adobe.io/.../short-url/...` scaricabile con `curl -L`.
- **OG image via convention Next `opengraph-image`**: file statico `src/app/(public)/<route>/opengraph-image.jpg` (1200×630) + `opengraph-image.alt.txt` → Next genera **automaticamente** `og:image` **e** `twitter:image`, servendolo a un **URL hashato** (`/<route>/opengraph-image-<hash>.jpg?...`). ⚠️ il path semplice dà **404** (normale: usare l'URL del meta). Coesiste con `metadata.openGraph` (title/description/url) della pagina. **Finding ricorrente**: il sito non aveva **nessuna** OG image (condivisioni social senza anteprima) → aggiungere una OG default nel root layout (follow-up). Nano Banana 2 rende bene il **testo** dei banner branded; l'utente può comunque fornire la propria.
- **Regole brand mascotte (in `NINO.md` §10 + memoria)**: mascotte **in sella → casco obbligatorio**; bici → **ruote mag a 5 razze** nero opaco (mai raggi a filo). Includere nei prompt `nano_banana_2` e verificarle in review.
- **MCP Higgsfield instabile — "connection lost" spesso accoda comunque il job**: una `generate_image` che ritorna `MCP server connection lost` ha **spesso già accodato** il job (risposta persa). **NON ritentare a raffica** (doppioni + crediti sprecati) → verificare con `show_generations` (mostra solo i **completati**; i pending non compaiono → attendere). `job_display` su un id mostra anche `failed`/`pending`. Preview headless (`Claude_Preview`) **flaky** (resta su `/` con sole HEAD) → verifica affidabile = **curl HTML SSR** (dev e prod) + smoke utente (riconferma EVO-022).
- **Riconoscere quando una sezione esistente già soddisfa il nuovo design**: il funnel "Come iscriversi" (EVO-022) era **già** il design v3 (connettore numerato + mockup + copy parent-focused) → mantenuto invariato (solo aggiunto il duo nella banda). Prima di riscrivere una sezione "da restylare", verificare se è già allineata al target — evita churn/regressioni.
- **Ordine "parent journey" + densità mascotte bilanciata** (esito `design-critique`): per una vetrina scuola rivolta ai genitori, ordine offerta → fiducia → appartenenza → valore pratico/sicurezza → social proof → funnel → CTA; tenere le sezioni di credibilità adulta (Filosofia/Maestri) sobrie/fotografiche **senza mascotte** per non scadere nel "troppo cartoon" (il decisore è il genitore).
- **Galleria mobile → scroll orizzontale**: per foto che su mobile diventano "incolonnate", carosello `flex overflow-x-auto snap-x snap-mandatory` + card `w-[82%]` (peek) + hint "Scorri →" + `tabIndex={0}`/`aria-label` (a11y), scrollbar nascosta; desktop invariato (`hidden sm:grid` / `sm:hidden`). Server Component, zero JS.

### Pattern appresi in EVO-030 (2026-06-23)

> Profilo genitore obbligatorio: step condizionale "I tuoi dati" nel wizard iscrizione + guard server-side + banner soft. _(Sviluppata/mergeata come "EVO-029" in PR #83 — `ce10a1a` — per collisione di numerazione; rinumerata EVO-030 in codebase e docs. Vedi nota in `evolutive/EVO-030-...md`.)_

- **Wizard a sequenza di step DINAMICA per chiave, non per indice**: quando un wizard può guadagnare/perdere uno step condizionale (es. "I tuoi dati" solo se profilo incompleto → 7 o 8 step), NON indicizzare la logica con numeri (`step === 4` per il create, `step <= 5` per il back-guard). Definire `type StepKey` + `STEP_LABELS` + `CORE_KEYS`, derivare `stepKeys = profiloCompleto ? CORE_KEYS : ["datiGenitore", ...CORE_KEYS]`, e confrontare sempre `currentKey = stepKeys[step-1]` (`currentKey === "tariffa"` per il create, `step <= idxOf("privacy")` per il guard). `idxOf(k) = stepKeys.indexOf(k)+1`. Così inserire uno step in testa NON sposta nessun offset: `computeResume`, `back`, `goNext`, `isNextDisabled`, render condizionale e `StepperWizard`/`StepHeader` (total dinamico) restano corretti. Supera il pattern "STEPS as const + step numerico" di EVO-026 (che si rompeva a ogni inserimento — vedi lezione "inserire uno step a metà wizard").
- **Gate dati come step condizionale interno, non onboarding forzato**: per rendere obbligatori dei dati "nel punto in cui servono" (qui: dati genitore per il tesseramento, raccolti all'iscrizione del figlio), si antepone uno step al wizard **solo se** un helper di completezza è falso, invece di bloccare l'utente al primo accesso. A profilo completo lo step sparisce (zero attrito). Un nuovo utente passa comunque dal gate al suo primo atto reale (iscrivere il figlio). Lo step salva via l'endpoint esistente (`PATCH /api/portale/profilo`) e poi avanza — niente nuova UI di profilo.
- **Guard server-side autoritativo + helper di completezza condiviso client/server**: il gate UI non basta (bypassabile con POST diretto). `isProfiloGenitoreCompleto(genitore)` in `portale-utils.ts` è la **single source of truth** usata da: (a) la server page del wizard per decidere se mostrare lo step, (b) il guard in `POST /api/portale/iscrizioni` che ri-legge il genitore e risponde **422 `PROFILO_INCOMPLETO`** se incompleto, (c) il banner dashboard. Pattern: ogni precondizione "bloccante" su una mutation deve avere il suo guard server-side, e la regola di completezza va in **un helper puro** condiviso, mai duplicata.
- **Componente campi controllato + validazione pura, condiviso tra due consumer**: `DatiAnagraficiGenitoreFields` (presentazionale, `values`/`onChange`/`errors`, prop `nomeCognomeReadonly`) + `validateDatiAnagrafici`/`isDatiAnagraficiValido` (pure, esportate dallo stesso modulo) sono riusate sia da `ProfiloGenitoreForm` sia dallo `StepDatiGenitore` del wizard. Lo stato vive nel parent (ognuno il suo), la validazione è un'unica funzione → zero duplicazione di campi/regole. Pattern per "stesso form in due contesti".
- **Validazione codice fiscale = charset + checksum, NON pattern posizionale**: `src/lib/codice-fiscale.ts` valida 16 alfanumerici (`/^[0-9A-Z]{16}$/` dopo trim+uppercase) + carattere di controllo (tabelle ODD/EVEN, `sum % 26` → `A`+resto). NON usare la regex posizionale stretta (`[A-Z]{6}[0-9]{2}...`): darebbe **falsi negativi sull'omocodia** (cifre sostituite da lettere) — il checksum invece resta valido. Esporre anche un messaggio d'errore (`ERRORE_CF`) e una `normalizeCodiceFiscale`. UI: helper "✓ Valido" verde quando valido, `FormError` quando no.
- **`WarningSoftBanner` come primitivo DS** (`src/components/ui/warning-soft-banner.tsx`): banner ember **non bloccante** (tile icona ember-500 + titolo ember-700 + descrizione + CTA ember-600 pari prominenza), responsive (`flex-col sm:flex-row`, CTA full-width su mobile). Promosso a componente DS riusabile (prima istanza: nudge "Completa il profilo" in dashboard). Distinto dal banner ember inline ad-hoc di EVO-018 (`<div role="alert">`): qui è un componente con prop `title`/`description`/`cta`/`icon`.
- **Verificare i numeri EVO liberi su `main` aggiornato, non al branch**: questa EVO ha cambiato numero **due volte** (027→029→030) per collisioni: EVO-027/028 erano già in main al branch, ed **EVO-029 è stato preso da PR concorrenti (#81/#82) mentre la feature era in volo**. Lezione di processo: il numero "libero" al branch può essere occupato da lavoro parallelo prima del merge. Mitigazioni: (a) ricontrollare `git ls-tree main evolutive/ | grep EVO-0NN` e i PR aperti **subito prima** del merge/docs, non solo al branch; (b) **codice e docs sono fonti separate** — se il codice è già mergeato sotto un label sbagliato (history immutabile), si allineano docs + commenti in-code al numero canonico nella PR docs e si annota il mismatch nella scheda, senza riscrivere la history.
- **Rebase pulito di un branch finito sul base sbagliato**: un branch nato per errore da un tip stale (vecchio main + commit già mergeati altrove) si ri-basa su `main` **senza perdere il working tree**: backup di tutti gli untracked + `git stash` dei tracked → `git reset --hard main` (gli untracked non-collidenti sopravvivono; i collidenti vanno rimossi prima, già backuppati) → ripristino selettivo dei soli file della task. Tenere fuori dai commit feature i file non correlati (no `git add -A`, riconferma EVO-026).

### Pattern appresi in EVO-031 (2026-06-24)

> De-indicizzazione `*.vercel.app` + canonical sul dominio reale (chiude D-27): `SITE_URL` → apex + 308 prod-only nel middleware.

- **`SITE_URL` (`src/lib/seo.ts`) è l'UNICO punto da cambiare per il dominio canonico**: da `SITE_URL` derivano `metadataBase` (root layout → `canonical` + `og:url` di ogni pagina), `robots.ts` (`host` + `sitemap`) e `sitemap.ts` (tutti gli URL). Spostare il canonical dal `vercel.app` al dominio = una riga + commento. Verifica residui: `rg -n "vercel\.app|trionoracing-next" src/` deve restare pulito (eccetto il nome npm package in `package.json`, che NON è un URL). Se emergono URL hardcoded (JSON-LD, manifest, OG custom), centralizzarli su `SITE_URL`/`absUrl()`.
- **Host-canonicalization redirect env-gated DENTRO il middleware esistente, non un secondo middleware né `next.config.redirects`**: per far uscire gli host `*.vercel.app` di **produzione** dall'indice Google, check in **cima** a `clerkMiddleware` (prima della logica auth): `if (process.env.VERCEL_ENV === "production" && host.endsWith(".vercel.app") && host !== CANONICAL_HOST) return NextResponse.redirect(url, 308)` con `url.host = CANONICAL_HOST` (preserva `pathname` + `search`). `CANONICAL_HOST = new URL(SITE_URL).host` → single source of truth. **Perché env-gated sul middleware e non regole `next.config` per-host**: `VERCEL_ENV === "production"` prende **tutti** gli alias di produzione (`<project>.vercel.app`, alias progetto/team, `*-git-main-*`) senza enumerarli, e lascia intatte le **preview** (`VERCEL_ENV === "preview"`) che devono restare navigabili per i test. Le regole `next.config` per-host non sono env-gated → richiederebbero l'elenco esplicito dei soli host di produzione.
- **Il matcher del middleware esclude `.xml`/`.txt`**: `robots.txt`/`sitemap.xml` serviti sul `vercel.app` NON vengono redirette (fuori dal matcher Clerk), ma — derivando da `SITE_URL` — emettono comunque URL sul dominio: innocuo per il de-index (le **pagine HTML** fanno 308 e il `canonical` punta all'apex). Non allargare il matcher solo per questo.
- **Verifica "preview non redirezionata" = 401 deploy-protection, NON 308**: una preview `*.vercel.app` con Vercel Deployment Protection attiva risponde **401** ai non autenticati (Vercel SSO intercetta prima del middleware). L'importante è che **non** sia un `308` verso l'apex: il 401 conferma che l'env-gating funziona (un utente Vercel autenticato vede la preview live). Trovare l'URL preview di una PR: `list_deployments` MCP Vercel → la entry con `target: null` e il `githubPrId` giusto.
- **Apex senza `www` come canonico; `www`→apex 308 è rischioso e rimandabile**: l'apex (`trionoracing.it`) è la forma che compare in Google. Un 308 `www`→apex consoliderebbe il doppione, ma rischia di rompere le **sessioni Clerk** (cookie host-scoped) → rimandabile: il `canonical` su apex consolida comunque `www`→apex lato Google senza redirect. Se si fa, verificare niente loop (l'apex deve restare 200) e niente logout su `www`.
- **Smoke SEO post-deploy via curl, dopo aver confermato il flip del deploy**: il nuovo build è live quando `curl -sI https://<project>.vercel.app/` passa da **200** a **308** (il vecchio deploy serve ancora 200 per qualche minuto). Solo allora i curl di verifica sono affidabili: `-I` per status/`location` (308 + path/query preservati), `curl -s … | grep canonical|og:url` sull'apex, `robots.txt`/`sitemap.xml` per host. Le route auth-gated non sono verificabili via curl (login Clerk) → smoke utente.

### Pattern appresi in EVO-032 (2026-07-08)

> Descrizione transazioni SumUp con riferimento bambino/causale (era hardcoded "Pagamento iscrizione Triono Racing" per tutte le transazioni).

- **Descrizione PSP arricchita col beneficiario reale, zero fetch aggiuntivi**: `descrizioneCheckoutSumUp(titolo, iscrizione)` in `portale-utils.ts` compone `"{Nome} {Cognome} — {causale}"` leggendo il nome dai lookup Airtable dell'iscrizione (`"NOME_BAMBINO (from TABELLA_BAMBINI)"?.[0]`, fallback ai campi piatti `NOME_BAMBINO`/`COGNOME_BAMBINO`) e riusando `titoloLabel(titolo).primary` (helper EVO-015) per la causale, invece di duplicare la logica di label. Fallback alla stringa generica originale se il nome è vuoto (nessuna regressione). `titolo`/`iscrizione` erano già caricati a monte nella route per l'ownership check → **zero round-trip Airtable aggiuntivi**. Pattern riusabile per qualsiasi campo `description`/`reference` verso PSP o servizi esterni che benefici di un riferimento umano-leggibile.
- **Smoke test E2E rimandabile al preview deploy per cambi a stringa isolata con fallback sicuro**: quando la modifica tocca un solo valore (qui: una `description` inviata a un payload esterno) senza alterare altri rami logici (reference, idempotenza, sconti invariati), e la verifica end-to-end richiederebbe credenziali/dati reali non disponibili all'executor (login genitore + pagamento vero), è legittimo — su decisione esplicita dell'utente — verificare offline la logica pura (casi: lookup presente / fallback campi piatti / fallback generico) + il comportamento runtime della route (compila, risponde correttamente senza auth), e rimandare il test E2E reale al preview deploy della PR prima del merge, invece di bloccare il ciclo in attesa di dati di test.
- **`verify-implementation` puntata su un altro progetto — riconoscerlo dai path/regole nel suo output**: riconferma pattern EVO-010/EVO-024 (skill non sempre caricata/coerente in sessione): qui la skill era configurata per un progetto diverso ("Cycling Experience", path e regole estranee). Riconoscerlo subito dal Passo 1 del suo output e produrre un report manuale con la stessa struttura per dimensione (Compliance funzionale / Convenzioni / Design system), applicando le convenzioni reali del progetto attivo — non le regole del progetto sbagliato.
